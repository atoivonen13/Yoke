# KN LodeRunner — study log

Dense late-time 9-band forecasting. Metric: **late-time RMSE (mag)**, phase
2 d < t ≤ 10 d, unless noted (studies 095–100 use 2 d < t ≤ 7 d; see 🔻 banner).
Eval via `eval_dense_latetime_9band.py`. **Current champion: study 123, 1.2521
@1000 on 2→10** (color-anchored SED head — COLOR_ANCHORED_HEAD, COLOR_SED_RANK=2,
Dt-dependent — at 100 epochs, backbone ON + DECODER unfrozen, waist 8, quantile
head, delta OFF, anchor OFF, rollout OFF). Prior: study 106 (1.4178), study 111
(1.3800).

**Why this file exists:** `studyIDX` is a launch-time template placeholder
(`<studyIDX>` in `training_input.tmpl`), filled on the cluster and written only to
rundir / CSV filenames — it is **never committed**. So a commit cannot be mapped
to a study number from git alone. This log is the manual bridge: study number →
commit → config → result. Add a row when a run is launched; fill RMSE when it
returns.

Noise floor: run-to-run seed noise ≈ 0.05–0.07 mag; 1000-object bootstrap CI
≈ ±0.068. Treat differences below ~0.07 mag as noise.

| Study | Commit | RMSE | Key config | Notes |
|------:|--------|-----:|------------|-------|
| 130 | _this commit_ | **PENDING @1000 (2→10)** | **PRINCIPLED BAND LOSS WEIGHTS on the study-123 CHAMPION.** Base = 123 exactly: 116 color-anchored SED head (`COLOR_ANCHORED_HEAD=True`, `COLOR_SED_RANK=2`, `COLOR_SED_DT_INDEPENDENT=False`), 100 ep, realistic + dense-dense concat. Every knob added since 123 set to its no-op: `REDSHIFT_FOURIER_BANDS=0` + `REDSHIFT_PIVOT_DIRECT=False` (so `append_redshift` is off, as in 123), `COLOR_ZTF_TIE_TWINS=False`, `ADD_CROSSSTREAM=False`, `DENSE_DENSE_CONCAT=True`, `REALISTIC_TARGET_CONCAT=True`. Knob-by-knob diff vs the 123 commit (5f06415) confirms only the new `BAND_WEIGHTS_MODE` differs. SOLE change: `BAND_WEIGHTS_MODE="std"` replaces the hand-set weights (ztfg/r/i 2, u 3, g 2, r/i/z/y 1) with each band's train-only normalization std, rescaled to mean 1, computed at runtime after the stats load (rank-0 prints the resolved weights; `band_weights_mode` + `band_weights` in save-meta). Modes: `"manual"` (old tensor), `"std"`, `"none"` (equal weight in z-units). py_compile OK. | **Motivation: the manual weights (commit 893b277, 2026-08-20) have been carried unchanged through every study since and were never A/B-tested. Their stated reasons are stale: ZTF=2 because ZTF "lags in rollout" (rollout is off), u=3 for a ~−5 mag u under-fade measured on a pre-color-head, delta-on model (u bias is now −0.15..+0.73). Two structural points: (a) a quantile/pinball loss's per-band optimum is the band's conditional quantile regardless of that band's weight, so weights cannot fix a bias — they only reallocate shared capacity; (b) with the color head's shared pivot `m_ref`, the weights decide which bands set the common level, so the 2–3× up-weight on the noisiest bands (u, g, ztfg) plausibly drives the band-wide bias swings of 125/127/128. `"std"` is the principled replacement: targets are z-scored per band, and pinball is linear in the residual, so `pinball_z × std_b = pinball in magnitudes` EXACTLY — the objective is in the metric's units with no hand-picked values. **Read @1000 (seed ±0.02) vs 123 (1.2521; ztfg 1.81/−0.73, u 1.90/−0.64, g 1.53/−0.02, r 1.32/−0.20, i 1.12/−0.33, z 1.01/−0.34, y 0.91/−0.22): (1) overall ≤ 1.2521 + 0.02 (a tie is a WIN here — same accuracy with an untuned knob removed)? (2) does the uniform negative bias shrink (less pull from the up-weighted noisy bands on the shared pivot)? (3) which bands move — compare against the resolved std weights in the rank-0 log.** If it ties or wins → adopt `"std"` as the recipe default. If it regresses → the manual weights were doing real work; follow up with `"none"` to see whether it's the up-weighting or the ratio that matters. Independent of 129 (different base).** |
| 129 | _this commit_ | **PENDING @1000 (2→10)** | **ALL-DENSE TARGETS — drop the realistic-target part.** Built ON 127 (125 recipe + `ADD_CROSSSTREAM=True`, `DENSE_DENSE_CONCAT=True`). SOLE change: new knob `REALISTIC_TARGET_CONCAT=False`, which omits the realistic-context/realistic-target part. Train set = dense-dense + cross-stream only, so EVERY target is a dense point (the scoring distribution); the cross part keeps realistic CONTEXT, so the deployment mapping is still trained. Norm stats unchanged (computed from realistic train files directly, not from the dropped part). Validation unchanged (realistic-only). New guard raises if no parts are built; rank-0 prints per-part sample counts. `realistic_target_concat` in save-meta. Fresh train, 100 ep. py_compile OK. | **Motivation: 125/127/128 showed the bias LEVEL tracks the dense-target FRACTION of the mix (½ dense → under-fade, ⅔ dense → over-fade). Hypothesis: the realistic targets are floor-truncated (faint late points dropped), so they teach a bright level the dense-scored eval punishes. 127 over-faded because the ⅔ mix was still a compromise between two DIFFERENT target distributions; all-dense removes the compromise instead of re-tuning it. **Read @1000 (seed ±0.02): (1) are ALL band biases near 0 at once (mean\|bias\| < 125's 0.176), not just ztfg? (2) overall < 123's 1.2521? (3) ztfg spread (√(RMSE²−bias²)) stays ≤ 127's 1.59?** Expected ceiling is modest: bias is 2–9% of MSE in every prior run, bias-free floor ~1.20. If biases center but RMSE doesn't move → the level question is closed and the next lever must be SPREAD. If biases DON'T center → the bias isn't a target-truncation artifact at all. **Confounds to read around: (a) the realistic part is gone, so each epoch has fewer samples → fewer optimizer steps over 100 ep (compare the rank-0 part counts against 127's); (b) validation is still realistic-context/realistic-target, a distribution this run no longer trains on, so val loss will look worse and the eval's default best-val-loss checkpoint pick may land on an odd epoch. Run the eval both default AND with `--exact_epoch` at the final epoch.** |
| 128 | _this commit_ | **1.2663 (1000, 17589 pts, 2→10) — HYPOTHESIS FALSIFIED. +0.011 vs 127, +0.014 vs 123 (both inside seed ±0.02). ztfg win LOST: RMSE 1.59→1.781, bias +0.00→−0.890 (worse than 123's −0.73). Biases back to 125-like under-fade (ztfr −0.11, ztfi −0.18, u −0.15, g +0.36, r −0.18, i −0.20, z −0.34, y −0.18). Reverted `DENSE_DENSE_CONCAT=True`.** | **DROP the dense-dense concat part — isolate the cross-stream ztfg win from the Study-127 global over-fade.** Built ON 127 (125 recipe + `ADD_CROSSSTREAM=True`). SOLE change: new knob `DENSE_DENSE_CONCAT=False` in `train_LodeRunner_ddp.py`, which OMITS the dense-CONTEXT/dense-TARGET concat part. So the train set = realistic part + cross-stream part ONLY (the cross-stream part — realistic context → dense faint-late target of the same object — is now the SOLE supply of dense late-faint TARGET supervision, instead of 127's dense-dense + cross-stream double supply). Everything else identical to 127/125: 123 color head, pivot-direct redshift, batch 5, TERMINAL_STEPS=50/WARMUP=20 cyclic, 100 ep. `dense_dense_concat` recorded in save-meta. Fresh train. Validated locally: py_compile OK; the concat-gate elif chain checked (False → prints "dense-dense omitted"; True-but-no-files → falls to the existing no-files branch). | **Motivation: Study 127 CONFIRMED the cross-stream mechanism — ztfg RMSE 1.81→1.591, bias −0.73→+0.001, the plateau BROKEN and the first non-sharing lever ever to move faint ztfg — but netted only a champion TIE (1.2549 vs 123's 1.2521) because it introduced a NEW global over-fade: every band's bias flipped negative→positive (g −0.02→+0.80 with RMSE 1.53→1.74 the worst), mean\|bias\| 0.332→0.345. Hypothesis: the over-fade is driven by DOUBLED dense late-faint TARGET supervision (dense-dense part PLUS cross-stream part both feed fainter late targets), which over-teaches the fade globally — nailing the extreme under-faded case (ztfg) but overshooting the mildly-under-faded Rubin bands. Dropping the dense-dense part removes half that dense-target supply. **Read @1000 (seed ±0.02): (1) is the ztfg win RETAINED (RMSE still ~1.59, bias near 0)? (2) do the Rubin biases pull BACK toward 0 (g/u/r/i/z/y over-fade reduced from 127's +0.8/+0.7/+0.3/...)? (3) overall < 123's 1.2521 (the net-champion test)? (4) mean\|bias\| < 127's 0.345, ideally < 125's 0.175?** If (1)+(2)+(3) all hold → the doubled-supervision hypothesis is right and this is a NET CHAMPION: the deployment mapping trained cleanly once the redundant dense-dense fade signal is removed. If ztfg REGRESSES back toward the plateau → the dense-dense part was actually carrying the ztfg fade signal and cross-stream alone is too sparse → keep dense-dense, instead DOWN-WEIGHT cross-stream (secondary option). If Rubin over-fade persists → the cross-stream part ITSELF over-teaches the fade (not the doubling) → the lever needs a fade-magnitude cap or ztf-only restriction, not a supervision-count fix.** **RESULT @1000: FALSIFIED on criterion (1). Overall 1.2663 (MAE 0.9416, vs 127's 0.8935). ztfg RMSE 1.781 / bias −0.890 — the plateau is BACK (worse than 123's −0.73). Rubin over-fade gone but overshot back to 125-like under-fade: ztfr −0.11, ztfi −0.18, u −0.15, g +0.36, r −0.18, i −0.20, z −0.34, y −0.18 (mean\|bias\| 0.287). None of the pre-registered branches fits cleanly: removing dense-dense DID pull the Rubin biases back (branch 2) but ALSO lost the ztfg win (branch "dense-dense was carrying the fade"). **Joint read of 125/127/128 — the bias LEVEL tracks the dense-target FRACTION of the training mix, not which dense part supplies it.** 125 (realistic + dense-dense, ~1/2 dense targets): under-fade. 127 (realistic + dense-dense + cross, ~2/3 dense targets): over-fade everywhere, ztfg centered. 128 (realistic + cross, ~1/2 dense targets): under-fade again, ztfg −0.89. The cross-stream part is NOT a better ztfg teacher than dense-dense at equal share — at equal dense fraction 128 ≈ 125. So 127's ztfg fix was largely a GLOBAL level shift from diluting the realistic (floor-truncated, bright-biased) targets, and ztfg only centered because it had the most under-fade to absorb; the same shift overshot every other band. The context-content mechanism may still be real but this A/B cannot separate it from the mix-ratio effect. **Bias-free decomposition (RMSE² − bias², n-weighted, from per-band summaries): 123 1.205, 125 1.249, 127 1.195, 128 1.230.** Bias carries only 2–9% of MSE in every run; the residual SPREAD is the ceiling and no mix change moved it more than ~0.05. ztfg spread is the one exception: 1.65 (123) → 1.59 (127) → 1.54 (128) — cross-stream does tighten ztfg scatter even when its level is wrong. Reverted `DENSE_DENSE_CONCAT=True` (127 config retained as the cross-stream baseline). **NEXT candidate: the realistic targets are the problem, not the dense ones.** Realistic-stream TARGETS are floor-truncated (faint late points dropped), so they bias the model bright; the eval scores DENSE truth. The direct test is to drop the realistic-TARGET part and train on dense-dense + cross only (every target dense = matches the scoring distribution; context stays realistic in the cross part, so the deployment mapping is kept). If that centers ALL bands (not just ztfg), the bias story is a target-truncation artifact. Caveat: the bias share is small, so the ceiling on even a perfect level fix is ~1.20, not a large win.** |
| 127 | _this commit_ | **1.2549 (1000, 17589 pts, 2→10) — MECHANISM FIRED, champion-TIE overall (+0.0028 vs 123's 1.2521, inside seed ±0.02; BEATS 125's 1.2628). The ztfg plateau is BROKEN — ztfg RMSE 1.81→1.591, bias −0.73→+0.001 (best ztfg of the whole program). BUT a new systematic OVER-fade appeared: every band's bias flipped negative→positive (u −0.64→+0.73, g −0.02→+0.80, r −0.20→+0.31, i −0.33→+0.28, z −0.34→+0.15, y −0.22→+0.14); z/i RMSE improved (0.94/1.07) but g regressed hard (RMSE 1.53→1.74, bias +0.80). mean\|bias\| 0.332→0.345 (worse). Net RMSE wash: ztfg win (−0.22) offset by g regression (+0.21) + fresh over-fade spread. Champion stays 123.** | **CROSS-STREAM TRAINING SAMPLES — attacks the faint-late ztfg plateau as a DATA-SUPPORT problem (the reframe forced by 117/118/126, memory `kn-anchor-wrong-granularity`/`kn-ztf-context-content-gap`).** Built ON the 125 recipe (123 champion color head + pivot-direct redshift). SOLE change: `ADD_CROSSSTREAM=True`. Adds a THIRD `ConcatDataset` part (does NOT replace the realistic or dense-dense parts) whose samples pair a REALISTIC-stream trailing-window context with a DENSE-stream late-faint target of the SAME object, on a shared per-object clock (`t0` = first realistic detection). Dataset change: new `target_data_glob` arg on `Kilonova_lc_scalar_context_DataSet_9band` enables cross-stream (window + single-step only); realistic stream relativized by its own `t0`, dense target stream relativized by the SAME `t0` (via new `_t0_per_file`), both normalized with the shared realistic stats; anchors enumerated on realistic events that can reach a dense target; target drawn by `_draw_target_idx_cross` (lead~U(0,8d) from the realistic anchor, argmin gap on the dense stream); `Dt = dense_target_t − realistic_anchor_t`. The window `x` is built by a NEW shared `_flatten_window` helper that BOTH `_getitem_window` and `_getitem_window_cross` call, so the model input is byte-identical in layout across single- and cross-stream. Train-only lever: no model-arg/tensor-shape change, no checkpoint-load-site edits; `add_crossstream` recorded in save-meta. Fresh train. Validated locally: py_compile both files + standalone torch replication (byte-identical `x` cross vs single; dense on the shared `t0` clock; appended phase = anchor_abs−t0; `Dt` = dense_abs−anchor_abs; one-hot target denorms to the raw dense mag; horizon draw spans multi-day leads; `target_data_glob=None` regression guard = pre-refactor inline layout). | **Motivation: horizon-EXACT diagnostic (`diagnose_ztf_horizon_exact.py`, 8000 objs; replicates `_draw_target_idx` via a Voronoi selection-prob, MC-validated) FALSIFIED the lead-coverage story and pinned the plateau to CONTEXT CONTENT. Lead IS covered: 72.6% of training weight on faint-late ztfg targets is at lead≥2d (median 3.22d). The gap: 78.3% of that weight pairs the faint target with a recent BRIGHT (<21) ztfg context point (ctx empty 0.0%), because in the single-stream concat context and target ALWAYS come from one stream. At eval the realistic context is empty/ztfg-free 51.7% of the time (ZTF ~21 floor). So the deployment mapping (sparse realistic context → dense faint-late target) is NEVER a training sample. This is a DATA lever, distinct from the dead cross-band SHARING levers (117/118/126) and global anchors (121) — it changes only which (context,target) pairs are sampled; same_seed guarantees the paired dense target is the SAME physical KN. **Read @1000 (seed ±0.02): (1) overall < 125's 1.2628 (ideally < 123's 1.2521)? (2) ztfg late RMSE down from ~1.78 with bias→0 (the mechanism target)? (3) good bands u/g/r/i/z/y NOT regressed (the recurring coupling failure mode)? (4) ztfg late plateau eases in the dense plots under REALISTIC sparse context?** If ztfg improves but overall flat → ztf tail is a small fraction of scored points; still a real per-band win. If good bands regress → the cross-stream fade supervision is over-teaching; drop the dense-dense part in a follow-up A/B to isolate. If NOTHING moves → the faint-ztf tail is genuinely photometry-limited even with the right context, and the ztf lever is spent.** **RESULT @1000: overall 1.2549 — BEATS 125 (1.2628) and is a TIE with champion 123 (1.2521, +0.0028 inside seed ±0.02). Criterion (1) not a new champion, but the diagnostic-predicted mechanism fired DECISIVELY. Criterion (2) BEST ztfg of the program: RMSE 1.81→1.5911 (−0.22), bias −0.73→+0.0013 — the plateau is BROKEN, cross-stream pairing taught the sparse-context→faint-target mapping exactly as designed. This VINDICATES the context-content diagnosis (`kn-ztf-context-content-gap`) and the DATA-lever reframe (`kn-anchor-wrong-granularity`): supplying the missing (context,target) pair worked where 3× cross-band sharing (117/118/126) failed. Criterion (3) FAILED — a new systematic OVER-fade: every band's bias flipped negative→positive (u −0.64→+0.73, g −0.02→+0.80, r −0.20→+0.31, i −0.33→+0.28, z −0.34→+0.15, y −0.22→+0.14, ztfr +0.41, ztfi +0.27). z/i actually IMPROVED (RMSE 1.01→0.94, 1.12→1.07 — the most-under-faded deep bands absorbed the fade correctly) but g regressed hard (RMSE 1.53→1.74, bias +0.80) and u worsened (1.90→1.96). mean\|bias\| 0.332(123)/0.175(125)→0.345(127) — the cross-stream dense-target supervision (dense-dense part PLUS the new cross part, both feeding fainter late targets) overwhelmed 125's pivot-direct bias-correction and over-taught the fade globally, nailing the extreme case (ztfg) while overshooting the mildly-under-faded Rubin bands. Net RMSE is a wash: the ztfg gain (−0.22) is cancelled by the g regression (+0.21) and the fresh over-fade spread; and (per the 125 lesson) bias→0 reshuffles residual signs without shrinking the variance-dominated tail. **VERDICT: the cross-stream lever is REAL and the mechanism is confirmed — first thing to ever move faint ztfg without cross-band sharing — but as configured it trades a ztfg win for a Rubin-band over-fade, netting a champion tie. Champion stays 123 (1.2521). NEXT (pre-registered from this study's plan): the over-fade is the fingerprint of TOO MUCH dense-target fade supervision; run the follow-up A/B that DROPS the dense-dense part and keeps ONLY realistic + cross-stream, to isolate whether the cross-stream pairs ALONE fix ztfg without dragging the good bands over-faint. If the over-fade is really driven by the doubled dense supervision, that A/B should keep the ztfg win while pulling the Rubin biases back toward 0 → a possible net champion. Secondary option: down-weight the cross-stream part rather than dropping dense-dense.** |
| 126 | _this commit_ | **1.3445 (1000, 17589 pts, 2→10) — FAILED, broad REGRESSION (+0.082 vs 125's 1.2628, +0.092 vs 123 champion). The tie barely moved its target (ztfg RMSE 1.78→1.75) and made 8/9 bands WORSE, including all THREE twins g/r/i. Reverted `COLOR_ZTF_TIE_TWINS=False`. Third confirmation faint-ztfg resists structural sharing (117/118/126).** | **ZTF-TWIN SED-SLOPE TIE — attacks the ztfg/r/i late-time PLATEAU (the faint-tail variance the 125 pivot-direct level term could NOT compress).** Built ON the 125 recipe (123 champion color head + pivot-direct redshift). SOLE change: `COLOR_ZTF_TIE_TWINS=True`. In the color-anchored head, alias the ztf bands' SED-slope rows to their near-identical Rubin twins: `W_band[ztfg]:=W_band[ps1_g]`, `W_band[ztfr]:=W_band[ps1_r]`, `W_band[ztfi]:=W_band[ps1_i]` (row-gather via a non-persistent index buffer). Result `color[ztf] = color[twin] + (b_ztf − b_twin)`: the ztf band inherits the twin's z_sed-driven fade shape EXACTLY, offset only by a free per-band zero point (`b_band` stays fully free for all 9). Parameter set unchanged (ztf slope rows 0/1/2 become unused, zero-grad) → old checkpoints strict-load; forward() only ties when the flag is on, so the flag is round-tripped in checkpoint meta + all 4 load/eval sites (default False). Fresh train. Validated locally: py_compile all 6 files + standalone torch replication (shape; tied ztf−twin color diff CONSTANT across objects vs varies untied; colors sum≈0; monotone quantiles; ztf-only masked loss puts grad on the TWIN row and ZERO on the aliased ztf row). | **Motivation: the 125 dense plots are unambiguous — ztfg/r/i plateau 2–8 mag too bright at late times (truth often falls BELOW the p10 band → confidently wrong), while their near-identical Rubin twins (g/r/i, same passband) track the fade correctly (lc_00137: ztfg stalls ~27 vs g→~33, both truths→~35; lc_00103: ztfg truth below p10). Root cause: ZTF's ~21 detection floor drops the faint ztf detections, so training never supervises the faint ztf tail and its free W_band slope row decouples/flatlines. The color head's rank-2 bottleneck did NOT fix this because each band still has its own free slope row. Tying the slope to the well-supervised twin makes the plateau structurally impossible: ztf fade = twin fade. This is a ZTF-per-band lever (memory `kn-anchor-wrong-granularity`: faint-ztfg fix must be ztf-ONLY, not global), so it should not touch the 6 good bands. **Read @1000 (seed ±0.02): (1) ztfg RMSE 1.78 → toward g's ~1.5? (2) overall < 125's 1.2628 (ideally < 123's 1.2521)? (3) ztfr/ztfi improve and g/r/i (twins) NOT dragged? (4) ztf-band late plateau gone in the dense plots (ztf tracks its twin's fade)?** If ztf improves but overall flat → the ztf tail is a small fraction of scored points; still a real per-band win. If g/r/i regress → the twin's slope is being pulled by the ztf zero-point offset (shouldn't happen; b_band is free) → revisit. **RESULT @1000: FAILED. Overall 1.3445 = +0.082 vs 125 (1.2628), +0.092 vs 123 champion (1.2521) — well beyond seed ±0.02, a real broad regression. Per-band: ztfg 1.780→1.749 (barely moved, the target), ztfr 1.180→1.226, ztfi 0.980→1.111, u 1.955→2.032, g 1.597→1.698, r 1.388→1.420, i 1.102→1.211, z 0.991→1.083, y 0.873→1.070 — 8 of 9 bands REGRESSED, including all THREE twins g/r/i and the deep-blue u. The predicted failure mode (criterion 3) FIRED: aliasing W_band[ztf]:=W_band[twin] made the twin's slope row receive gradient from BOTH bands, which are supervised on DIFFERENT phase distributions (ztf photometry is mostly early — ZTF floor drops the late points — while Rubin keeps late detections), so the shared row became a compromise fit for neither, corrupting the calibrated twin's fade shape rather than lifting ztfg to it. My prediction that b_band's freedom would insulate the twins was WRONG: the offset (b) is free but the SLOPE (W row) is exactly what got shared, and slope is what carries the fade shape. So the hard-tie hypothesis is wrong at the parameter level — forcing a shared slope AVERAGES two differently-supervised bands and loses both, it does not transfer g's good behavior to ztfg. This is the THIRD confirmation that faint-ztfg resists structural cross-band sharing (117 Dt-dependent code +0.054, 118 Dt-independent code, 126 twin-tie +0.082 — all die on the same variance/support trade). Reverted `COLOR_ZTF_TIE_TWINS=False`; machinery kept (default False, harmless, strict-loads). Champion stays 123 (1.2521); 125 remains the bias-corrected tie. IMPLICATION: the faint-ztf problem is a DATA-SUPPORT problem (no late faint ztf labels), not a parameter-coupling problem — no architectural sharing lever can manufacture the missing supervision. Next lever must either (a) supply the missing signal (e.g. a phase-aware ztf prior/anchor that only acts where ztf has no data, per-band) or (b) accept ztf tail as an irreducible floor and target a different residual.** |
| 125 | _prev commit_ | **1.2628 (1000, 17589 pts, 2→10) — RMSE NO-OP (+0.011 vs 123's 1.2521, inside seed ±0.02) BUT THE MECHANISM FIRED: the shared under-fade bias was nearly HALVED (mean \|bias\| 0.332→0.175) and MAE dropped 0.9286→0.9088. Opposite of 124 (which made the bias worse). Redshift IS the level knob; pivot-direct IS the right delivery path — it just doesn't convert to an RMSE win.** | **PIVOT-DIRECT redshift — the pre-registered escalation after conditioner-path redshift (124) died.** Built ON the study-123 CHAMPION recipe (116 color-anchored SED head: `COLOR_ANCHORED_HEAD=True`, `COLOR_SED_RANK=2`, `COLOR_SED_DT_INDEPENDENT=False`, 100 ep, decoder scope, waist 8, N_QUANTILES=3 weights (1,3,1), delta/anchor/rollout OFF, batch 5, CONTEXT_WINDOW_DAYS=2, TARGET_HORIZON_DAYS=8, DROP_UPPER_LIMITS=True). **SOLE change vs 123: `REDSHIFT_FOURIER_BANDS=0` (Fourier conditioner path OFF — that was 124's dead path) + new `REDSHIFT_PIVOT_DIRECT=True`.** The new flag adds a single learned scalar `redshift_pivot_weight` and computes `m_ref ← m_ref + w_z · z_std` in the color head, where `z_std=(z−0.01424)/0.00365` is the standardized redshift and the shift is applied AFTER the monotone-quantile construction so it moves EVERY band and EVERY quantile by the SAME amount (a pure distance-modulus level offset; colors untouched). `w_z` init 0 → byte-identical to 123 at init. Raw z is still the second trailing scalar (dataset `append_redshift` and all builders now gate on `fourier>0 OR pivot_direct`, so z reaches the model with the Fourier bank off). Threaded through checkpoint meta + all 4 load/eval sites (`redshift_pivot_direct` key, default False → old checkpoints strict-load). Fresh train (new head scalar; backbone loads as always). Validated locally: py_compile all 7 files + standalone torch replication (shape, quantile monotonicity, colors sum≈0, w_z=0≡no-z head, w_z≠0 shifts all bands/quantiles by exactly w_z·z_std, gradient flows to w_z from a single-band-masked loss). | **Motivation: 124 proved the level signal is NOT reaching the pivot through the pooled conditioner waist — z was ignored (or used counterproductively; the shared bias got WORSE). Distance modulus is a pure band-shared level offset = exactly what the pivot `m_ref` represents, so wire it there DIRECTLY with nowhere to leak. This is the LAST redshift wiring. Read @1000 (seed ±0.02): (1) overall < 123's 1.2521? (2) does the UNIFORM negative bias (ztfg −0.73, u −0.64, z −0.34, i −0.33, ztfi −0.30, ztfr −0.21, y −0.22, r −0.20) move TOWARD 0 — the mechanism target? (3) good band g (~0) not pushed negative / good bands not regressed? If the shared bias STILL won't move toward 0 even with z hard-wired onto the level term, the late-time level residual is NOT redshift-addressable via this architecture → CLOSE the redshift thread (memory `kn-redshift-feature-idea`).** **RESULT @1000: overall 1.2628 = +0.011 vs 123 (inside the ±0.02 seed floor, so an RMSE no-op) — FAILS read-criterion (1). BUT criteria (2)/(3) PASSED decisively, the opposite of 124: the uniform negative bias moved TOWARD 0 across the board — ztfg −0.73→−0.54, u −0.64→−0.30, i −0.33→−0.01, ztfi −0.30→−0.08, z −0.34→−0.20, y −0.22→−0.09, ztfr −0.21→−0.12; r −0.20→+0.09 and g −0.02→+0.15 crossed to slightly too-faint (mild g overshoot, the only regression direction). Mean \|bias\| 0.332→0.175 (~halved); MAE 0.9286→0.9088 (−0.020). So `w_z` learned a REAL nonzero distance-modulus level term and the mechanism is confirmed — redshift IS the level knob and the pivot IS where it belongs (124's conditioner path could not do this). **Why bias↓ but RMSE flat: the shared level was already ~right on average in 123; correcting per-object level shuffles the sign of residuals (bias→0) without shrinking their spread, and late-time RMSE is dominated by the high-variance faint tail (ztfg/u RMSE still ~1.8/2.0), which a single band-uniform level term cannot compress.** This CONFIRMS the long-standing read: faint ztfg/u are VARIANCE/tail-limited at this point, not shared-level-limited — the level lever is now spent. **Redshift thread: mechanism validated, no RMSE win → keep pivot-direct in the champion recipe as a bias-correcter (free, w_z init 0 so it never hurts) but STOP chasing redshift for RMSE. Champion stays 123 (1.2521); 125 is a bias-improved tie.** Next lever must attack the faint-tail VARIANCE (ztfg/u), not level.** |
| 124 | _prev commit_ | **1.2756 (1000, 17589 pts, 2→10) — NO-OP AT BEST (+0.024 vs 123, at the seed-noise edge), and the bias moved the WRONG WAY. Always-on conditioner-redshift does NOT act as the level knob. Thread not dead, but conditioner-path wiring is.** | **RESULT: FAILED its read criteria. Overall 1.2756 vs the 123 champion 1.2521 = +0.0235 (~ the ±0.02 seed floor, so no better than noise). CRITICALLY, the shared negative under-fade bias — the whole reason to add redshift — got MORE negative, not less: ztfg −0.73→−1.00, u −0.64→−0.93, ztfr −0.21→−0.35, r −0.20→−0.29, z −0.31→−0.31 (flat), g −0.02→−0.08. The two worst bands (ztfg, u) drifted ~0.27–0.29 mag FURTHER too-bright. So redshift-in-the-conditioner is not being used as a distance-modulus level term; it's ignored (and the drift is seed/train variance) or used counterproductively. NOT a data-poverty problem: distance modulus across this population has 0.72 mag STD (p5–p95 spread ~2.1 mag; z mean 0.01424 std 0.00365) — the level signal is genuinely there, the conditioner path just doesn't route it to the pivot. **Conditioner-path always-on redshift is DEAD. Before abandoning the redshift thread, try the two things the plan pre-registered as the escalation: (1) PIVOT-DIRECT wiring — feed the standardized z straight into `ref_head` (an additive level term on `m_ref`) instead of only through the pooled conditioner waist, so the model can't help but use it as a level; (2) an oracle ceiling-probe sanity check that ANY head can exploit z here. If pivot-direct also fails to move the shared bias toward 0, the level residual is not redshift-addressable via this architecture and the thread closes.** Champion stays 123 (1.2521).** | **REDSHIFT AS A CONDITIONING SCALAR (the PRIMARY remaining lever; memory `kn-redshift-feature-idea`).** Built ON the study-123 CHAMPION recipe: 116 color-anchored SED head (`COLOR_ANCHORED_HEAD=True`, `COLOR_SED_RANK=2`, `COLOR_SED_DT_INDEPENDENT=False`) at **100 epochs**, champion base (decoder scope, waist 8, N_QUANTILES=3 weights (1,3,1), delta/anchor/rollout OFF, batch 5, CONTEXT_WINDOW_DAYS=2, TARGET_HORIZON_DAYS=8, DROP_UPPER_LIMITS=True). SOLE addition: `REDSHIFT_FOURIER_BANDS=8` (`REDSHIFT_MEAN=0.01424`, `REDSHIFT_STD=0.00365`). Redshift is the SECOND trailing scalar (fixed order [events, phase, redshift]); the dataset reads it raw from each file's `injection_parameters` JSON and the model STANDARDIZES `(z−mean)/std` inside `_encode_redshift` (tiny range ~[0.001,0.019] can't feed a day-scaled bank) → sin/cos bank + 1 linear channel, concatenated onto the conditioner input. New knob threaded through checkpoint meta + all 4 load/eval sites (default 0 = disabled, byte-identical, old checkpoints strict-load). ALWAYS-ON (train + forecast); optional-at-forecast via train-time dropout is a DEFERRED follow-up. Redshift IS passed at forecast — `eval_dense_latetime_9band.py` reads it per-object from `injection_parameters`, so the @1000 eval measures the always-on ceiling directly. Fresh train (new conditioner first-layer width can't load 123 head weights; backbone still loads). CSV row 124 = champion-family launch regime (batch 5, TERMINAL_STEPS=50/WARMUP=20, cyclic, total_epochs=100). | **Motivation: `aux_loss_precheck.py` proved faint ztfg is a LEVEL problem, not variance (marginal std 0.556; error dominated by bias/plateau). Color coupling CLOSED (117/118 dead). Distance modulus = apparent−absolute mag is monotone in redshift → redshift is the direct LEVEL knob for the under-fade, reaching the pivot `m_ref` through the pooled conditioner waist.** **KEY: study 123 (the new 1.2521 champion) exposed a UNIFORM NEGATIVE bias across almost every band (ztfg −0.73, u −0.64, z −0.34, i −0.33, ztfi −0.30, ztfr −0.21, y −0.22, r −0.20; only g ~0) — a SHARED positive-mag / too-bright offset. That shared offset is EXACTLY what a distance-modulus level term corrects; redshift should lift the whole set toward 0.** Rung-1 CEILING PROBE vs study 123 (1.2521 overall, ztfg RMSE 1.81 / bias −0.73). **Read @1000 (seed ±0.02): (1) overall < 1.2521? (2) ztfg bias −0.73 → 0 (and RMSE below 1.81)? (3) the shared negative under-fade (u/i/z/ztfi) lifts toward 0 without over-correcting g?** If it clears → try pivot-direct wiring (redshift straight into `ref_head`) and/or the dropout path for optional-at-forecast. If flat → the level info isn't reaching the pivot through the waist; escalate to head-direct before abandoning. |
| 123 | _prev commit_ | **1.2521 (1000, 17589 pts, 2→10) — ★ NEW CHAMPION, −0.13 vs 116 (1.3891) AND 111 (1.3800), WAY beyond seed ±0.02.** | **LONGER RUN OF STUDY 116 (color-anchored SED head) — total_epochs 40→100, SOLE change.** Study 116 config EXACTLY: `COLOR_ANCHORED_HEAD=True`, `COLOR_SED_RANK=2`, `COLOR_SED_DT_INDEPENDENT=False` (Dt-DEPENDENT = the 116/117 variant, NOT the regressed 118 Dt-independent one), champion base (decoder scope, waist 8, N_QUANTILES=3 weights (1,3,1)=normalized (0.2,0.6,0.2), delta/anchor/rollout OFF, batch 5, CONTEXT_WINDOW_DAYS=2, TARGET_HORIZON_DAYS=8, DROP_UPPER_LIMITS=True). Only `--total_epochs 40→100` in training_START.input. Fresh train. Ran as `runs/study_123`. | **RESULT: the extra epochs delivered a LARGE, broad win — NOT just ztfg. Overall 1.2521 (vs 116's 1.3891 at 40 ep) — the color-anchored SED head KEPT improving well past 40 epochs. Per-band: ztfg RMSE=1.8054 bias=−0.73 (higher RMSE than the 40-ep 116's 1.68 but that run's ztfg was one point in a worse overall; here the whole curve dropped), ztfr 1.16/−0.21, ztfi 1.02/−0.30, u 1.90/−0.64, g 1.53/−0.02, r 1.32/−0.20, i 1.12/−0.33, z 1.01/−0.34, y 0.91/−0.22. THE INFORMATIVE SIGNATURE: bias is UNIFORMLY NEGATIVE (too bright / under-fade) across 8 of 9 bands (g the lone exception ~0) — a SHARED level offset, not per-band scatter. This is the cleanest evidence yet that a single LEVEL knob (distance modulus / redshift) is the right next lever: it should shift the whole set toward 0. ⚠️ Schedule stayed cyclic (LR bounded [0.5,1.0]×anchor, full cosine cycle ~1.2 ep) — NOT the study-109 single-anneal terminal_steps trap, so the 100-ep extension was a clean single-variable test. **Champion updated 116→123. Redshift (124) now builds ON this recipe and must beat 1.2521.** |
| 065 | _TBD_ | _TBD_ | _TBD_ | Prior baseline (late Aug). Commit not recoverable from git — please confirm. |
| 075 | `e6883db` | 1.8373 | bypass MLP, waist 32, point head, W=2.0/M=12, phase F=10, hidden 128 | Pre-quantile champion. "revert changes back to run 75". |
| 076 | `e6883db`* | 1.9372 | 075 + phase min-period 0.3 d | Regressed; min-period reverted to 1.0 d. |
| 077 | `260c524`* | 2.1137 | 075 + anchor-pinned subsample, W=2/M=12 | Regressed. (Also hit ModuleNotFoundError — new file not copied to cluster.) |
| 078 | `260c524`* | 2.1411 | subsample at wide window W=5/M=24 | Regressed further. Subsample abandoned; reverted to legacy trailing selection (option A). |
| 079 | `a1166b7` | 1.9054 | bypass MLP, waist 32, **point** head (N_QUANTILES=1) | Same commit as 080, point head. |
| 080 | `a1166b7` | **1.8582** (1000) / 1.8318 (100) | bypass MLP, waist 32, **quantile** head (N_QUANTILES=3, levels 0.1/0.5/0.9) | **Champion.** Median-pinball collapsed blue-band biases vs the point head. Calibrated bands (coverage 0.777). 100-obj number was optimistically biased (+0.026 vs 1000). |
| 081 | `9ef46e8`* | 2.0208 (100) | backbone ON (frozen), waist **8** (forced), quantile head | Regressed vs 080. Confound: waist narrowed 32→8 (non-bypass path needs backbone_channels=8). Biases returned (u −1.03, g −1.04) → underfitting. |
| 082 | _prev commit_ | 1.8492 (100) | backbone ON, **decoder tail unfrozen** (PatchExpand[-1] + up_connect[-1] + linear4unpatch) at 0.1× head LR, quantile head, waist 8 | Capacity test. Unfreezing the tail recovered **0.17 mag vs 081** → backbone path was capacity-limited, not dead weight. But only **ties** 080 at higher cost + waist-8 bottleneck. No reason to adopt. |
| 083 | _prev commit_ | 1.8900 (1000) / 1.8576 (100) | **bypass MLP, waist 64** (BYPASS_CHANNELS=64), quantile head, W=2.0/M=12 | Waist-width capacity test. **Ties** 080 at 1000 obj (Δ0.032, within noise), if anything slightly worse → waist saturates by 32; 64 is wasted capacity. Confirms the **aleatoric floor**. **080 (waist 32) stays champion** — and is the efficient one. |
| 084 | _prev commit_ | 1.9350 (100) | **backbone ON**, waist 8, **frozen encoder + fine-tuned bottleneck+decoder** @ 0.1× LR (scope="decoder", mult=0.1), quantile head, **BATCH_SIZE=2** (eff. 8) | **Regressed** ~0.10 vs 080 (fully trained 40 ep, best.pth). Every band under-fades (larger −biases than 080); worst RMSE g/r 2.48. But CONFOUNDED: batch dropped 5→2 to fit the full decoder's activations in VRAM, while 080/082/083 ran batch 5 (eff. 20) — so scope is tangled with a noisier/smaller-LR regime. 085 controls for it. |
| 085 | _this commit_ | 1.8939 (100) | **backbone ON**, waist 8, **tail unfrozen** (scope="tail", mult=0.1), quantile head, **BATCH_SIZE=2** (eff. 8) | **Batch-2 twin of 082 — the clean control for 084.** Two clean 1-variable reads: (1) SCOPE at fixed batch — 085 (tail) 1.8939 **beats** 084 (decoder) 1.9350 by 0.041 → unfreezing the whole decoder genuinely hurts, not just the batch. (2) BATCH at fixed scope — 082 (tail, batch 5) 1.8492 beats 085 (tail, batch 2) 1.8939 by 0.045 → the batch cut is real & material and stacks. Also: 085 biases far better-centered than 084 (ztfr −0.20 vs −0.63, u −0.19 vs −0.57, r −0.14 vs −0.76) → tail scope generalizes; full decoder drifts bright (under-fades). |
| 086a | _this commit_ | 2.9360 (100) | **SPATIAL RENDER** + backbone **frozen**, waist 8, quantile head | **Interface fix, not a config toggle.** Root cause of the 080-tie: 081–085 tiled the conditioner's [B,8] vector into a spatially-**constant** image and global-pooled the output, forcing the Swin backbone to act as a frozen per-channel MLP (the bypass MLP's exact job → ties by construction). 086 renders the light curve as a 2D field (rows=time, cols=band, bilinear tent splat/event) and **gathers** the prediction at the target's (row(Dt), band-col) instead of global-pooling — finally exercising 2D windowed attention on structured input. Conditioner/output_head dropped for a small `read_head`; output contract [B,Q,9] unchanged (rollout/eval need no change). 086a = frozen backbone (only read_head trains). **Catastrophic (+1.10 vs 080).** Persistence collapse: the gather reads rows 224–1119 (forecast region), which is ZERO by construction — only context rows 0–224 get splatted (~0.44% of the field non-zero). A frozen encoder cannot manufacture signal in the empty readout region. Root failure of the whole render line. |
| 087 | _this commit_ | 2.1899 (100) | **bypass MLP control** (SPATIAL_RENDER=False, BYPASS_BACKBONE=True), waist per config, quantile head | Bypass sanity control run alongside the render experiments (NOT the 080 champion config — differs in other flags). Beats render 086a by ~0.75 but still well above 080, i.e. render is far worse than plain bypass. Confirms the render interface is the problem, not the task. |
| 088 | _this commit_ | 2.4136 (100) | SPATIAL RENDER + **tail unfrozen** @ 0.1× LR (scope="tail") + **TREND_DECAY_ANCHOR**, waist 8, quantile head | Render + partial backbone unfreeze + trend anchor, the best attempt to rescue the render line. Recovered +0.52 vs 086a (2.94→2.41) — tail unfreeze lets the backbone push *some* signal into the forecast rows — but still loses to bypass 087 (2.19) and is ~0.55 above 080. Under-fades rather than collapses. **Render line abandoned after this:** even with unfreeze + anchor, gathering from a structurally-empty region can't beat the bypass MLP. |
| 089 | `cb2bba9` | 2.0153 (100) | **DENSE-CONTEXT probe** (PROBE_DENSE_CONTEXT=True), bypass MLP, **waist 8** (BYPASS_CHANNELS=None), trend anchor, quantile head, **BATCH_SIZE=2** | Ceiling probe for distillation: draw model context from the DENSE set (all in-window detections, truncated at 2 d — dense-within-window, not leakage), scored region unchanged so RMSE is comparable. **Go/no-go = does privileged dense context help?** Two confounds vs the 080 champion (see ⚠️ banner below): (1) waist 8 not 32; (2) **batch 2 not 5**. Compare only within the batch-2 family (090/091/092). |
| 090 | `cb2bba9` | 1.9616 (100) | dense-context probe, bypass MLP, **waist 8**, **TREND_DECAY_ANCHOR=False** (flat-hold), quantile head, **BATCH_SIZE=2** | 089 with the anchor flipped off. Within-regime read: (089 vs 090) at fixed dense+waist-8+batch-2, flat-hold beats trend by ~0.05. NOT comparable to 080 (batch + waist + anchor all differ). |
| 091 | `cb2bba9` | 2.0555 (100) | **DENSE-CONTEXT probe** — bypass MLP, **waist 32**, **trend anchor**, quantile head, **BATCH_SIZE=2** | Intended as the clean single-variable test vs 080, but **it is NOT** — 080 ran batch 5, this ran batch 2 (see ⚠️). ~~"Definitive no-go, dense regresses +0.22 vs 080"~~ **RETRACTED:** that compared batch-2 dense against batch-5 sparse; the offset was mostly the batch cut, present in every batch-2 run. The valid comparison is same-regime: **091 (dense, batch 2) 2.0555 vs 092 (sparse, batch 2) 2.1289 → dense slightly BEATS sparse by ~0.07** (within the ±0.068 @100 noise). Dense context is NOT harmful; distillation is NOT ruled out. Needs a batch-5 rerun (093+) to settle. |
| 092 | `19b6fd7` | 2.1289 (100) | sparse-context reproduction control, bypass MLP, **waist 32**, trend anchor, quantile head, **BATCH_SIZE=2** | Meant to reproduce 080 (~1.83). **Failed to — landed 2.1289 (+0.30).** Diagnosis: code path is byte-identical to 080 (dataset/epoch/eval/optimizer all verified unchanged); the drift is the **launch regime** — the active CSV row runs BATCH_SIZE=2, but 080 ran batch 5 (eff. 20). STUDY_LOG already records the batch 5→2 penalty as ~0.045 (082→085); here the full batch-2 + schedule effect is ~0.30. So 092 is "080's architecture, batch-2 regime," not a reproduction. **This run is what exposed the batch confound in 089–091.** Reproduction retried at batch 5 as 093. |
| 093 | `19b6fd7` | **1.8792 (1000)** / 1.9121 (100) | sparse-context reproduction of 080, bypass MLP, **waist 32**, trend anchor, quantile head, **BATCH_SIZE=5** | **✅ CLEAN REPRODUCTION of 080** (1.8582 @1000). Δ = **+0.021 @1000**, within noise. Confirms (1) no harness drift — current code + moved data + optimizer-builder refactor all reproduce the champion; (2) the batch confound was the whole 092 story (batch 2→5 recovered +0.22 @100); (3) the @100 residual vs 080 was eval-size optimism (1.9121@100 → 1.8792@1000). **Re-anchors the batch-5 reference regime.** One difference: bias signature flipped — several bands positive (u +0.14, g +0.12, ztfi +0.14) vs 080's persistent negative under-fade. Same RMSE, different minimum; the blue under-fade is largely gone here. **093 is now the sparse batch-5 baseline for the distillation go/no-go** (needs a dense batch-5 twin to compare, evaluated with --probe_dense_context). |
| 094 | `19b6fd7` | **1.8733 (1000)** | **DENSE-CONTEXT twin of 093** (PROBE_DENSE_CONTEXT=True), bypass MLP, **waist 32**, trend anchor, quantile head, **BATCH_SIZE=5** | **THE CLEAN DISTILLATION GO/NO-GO.** Identical to 093 in every knob (waist 32, trend anchor, batch 5); context source is the SOLE difference. Result: **Δ = −0.006 @1000 vs 093** (1.8733 vs 1.8792) — dense context is statistically **indistinguishable** from sparse, far inside the noise floor. **DISTILLATION = NO-GO, clean call.** Privileged dense in-window context buys nothing, so a dense-context teacher is no stronger than the sparse student → nothing to transfer. Confirms the late-time 2→10 d signal is genuinely absent from the 2 d window regardless of sampling density — an information limit, not capacity or modeling. This is the correctly-controlled version of the retracted 089–091 claim. |
| 095 | _TBD_ | 1.9168 (1000, 998 obj / 12485 pts) | **SHORTER HORIZON: scored 2→7 d** (TARGET_HORIZON_DAYS 8→5, eval `--late_time_max_days 7`), scheduled sampling **ON** (n_rollout_steps=12), sparse context, bypass MLP, **waist 32**, trend anchor, quantile head, **BATCH_SIZE=5** | ⚠️ **NOT comparable to ≤094** (those scored 2→**10**; this scores 2→**7**, an easier near-region population → different RMSE by construction, see 🔻 banner). Sole purpose was to establish the shorter horizon + serve as the scheduled-sampling-**ON** baseline for the 096 twin. Clean same-region comparison is **095 vs 096 only**. |
| 096 | _TBD_ | **1.7896 (1000, 998 obj / 12485 pts)** / 1.7104 (100, 1218 pts) | **scheduled sampling OFF** (n_rollout_steps 12→**1**), scored 2→7 d, sparse context, bypass MLP, **waist 32**, trend anchor, quantile head, **BATCH_SIZE=5** | **Rollout-off twin of 095 — clean single-variable test.** Everything matched to 095 (region, horizon, context, waist, anchor, batch); only scheduled sampling differs. Result: **Δ = −0.127 @1000 vs 095** (1.7896 vs 1.9168), well outside the ±0.02 @1000 noise → **scheduled sampling was HURTING.** 12-step rollout optimized for an autoregressive task never run at eval (direct single-pass) — paid its cost, never collected its benefit. Cleanest bias signature of the campaign (ztfi −0.08, i +0.04, z +0.21, y −0.06 near-centered; residual offenders are the known blue-band limit: u −1.29, g −0.41, ztfg −0.50, r −0.52). @100→@1000 gap (1.7104→1.7896, +0.079) confirms the eval-size optimism model. **Confirmed a genuine win by the 093-on-2→7 baseline below:** 096 beats the champion 1.8677 by −0.078 on the identical region. Caveat: 096 is a horizon-5 *specialist* (can't forecast 7→10 d); this is a win within the 7-day deployment target, not strict domination of the horizon-8 generalist. |
| 093′ | `19b6fd7` | 1.8677 (1000, 998 obj / 12485 pts) | **093 checkpoint re-scored on 2→7 d** (eval `--late_time_max_days 7`, no retrain), scheduled sampling ON (as-trained), sparse, waist 32, trend anchor, batch 5 | **The owed same-region baseline** — makes 095/096 comparable to the champion. Not a new training run; identical weights to 093, only the scored region shrank 2→10 → 2→7. **Δ vs 093-on-2→10 = −0.012** (1.8677 vs 1.8792): dropping the 7→10 d points barely helps → the 2→7 region is **NOT meaningfully easier**, so 096's low number is not a region artifact. **Δ vs 096 = +0.078** (096 wins): scheduled-sampling-off beats the champion on the matched region — a second, independent confirmation of the 095↔096 twin result. Bias all-positive here (ztfi +0.25, i +0.27, z +0.18, y +0.28, g +0.27) — different minimum than 096's blue-negative signature; same RMSE-class, different failure mode. |
| 097 | _TBD_ | 1.6119 (100, 1218 pts) | **TREND ANCHOR OFF** (rollout-off twin of 096 with anchor removed), scored 2→7 d, sparse, bypass MLP, **waist 32**, quantile head, PREDICT_DELTA=True, **BATCH_SIZE=5** | Anchor-off A/B vs 096 (@100: 1.6119 vs 1.7104 = **−0.099**, ~1.5σ at ±0.068). **Surprise: the analytic trend anchor was a CONSTRAINT, not a crutch** — the head fits late-time fade *better* without being pinned to a per-band local-slope extrapolation. Refutes the "anchor is doing the forecasting" hypothesis. Blue bands went *more* negative without the anchor pinning them (u −1.39, g −1.06) — win redistributes error but nets better. ⚠️ @100 only; not yet @1000. |
| 098 | _TBD_ | 1.7024 (100) @ep40; **2.3400 (100) @ep3** | **POINT HEAD + HUBER** (N_QUANTILES=1, LOSS_TYPE=huber δ=0.1), anchor off, delta on, 2→7 d, sparse, waist 32, batch 5 | **Diagnostic run — reverts the quantile head, expected to regress, and did** (~0.09 worse than 097's quantile head @100; blue biases returned u −1.77, g −1.34, as the 080 finding predicts). **KEY RESULT — resolves the "flat loss" mystery:** the ep3→ep40 eval descended **2.34 → 1.70 mag (−0.64!)**, so training was never dead. The flat training-loss curve was a plotting artifact of three stacked effects: (1) loss is in **normalized z-score units**, eval in mag; (2) **log y-axis** compresses the change; (3) **Huber δ=0.1 caps** the large-residual tail so the objective has a small numerical floor and "starts low." Fix: loss plot default flipped to **linear**. |
| 099 | _TBD_ | 1.6136 (100) | **PREDICT_DELTA OFF** (absolute head) + point/Huber (099 = 098 with delta off), anchor off, 2→7 d, sparse, waist 32, batch 5 | Delta-off A/B under the Huber head: 098→099 = 1.7024→1.6136 = **−0.089**. Mirrors the quantile-head delta-off effect (097→100, −0.088) → **delta-off is a real, head-independent win.** Absolute head fades better because the delta prior pinned forecasts to the bright near-peak last obs (structural under-fade). ⚠️ @100 only. |
| 100 | _TBD_ | **1.4738 (1000, 998 obj / 12485 pts)** / 1.5240 (100) | **NEW CHAMPION (2→7). = 096 with PREDICT_DELTA OFF.** Quantile head, anchor off, rollout off, absolute head, 2→7 d, sparse, bypass MLP, **waist 32**, **BATCH_SIZE=5** | **Biggest single jump in the log.** Δ vs 096 = **−0.316 @1000** (1.4738 vs 1.7896) — delta-off is the SOLE change from 096, so the full −0.316 is attributable to removing the delta persistence prior. Stacks with the quantile>Huber effect (−0.09) and rollout-off (−0.127). **The "aleatoric floor" (081–094) was substantially self-inflicted by the delta anchor**, which pinned forecasts to the bright near-peak last obs → chronic under-fade. Removing it let every band fade properly: cleanest @1000 bias signature ever (ztfg −0.05, y −0.07, r −0.25, z −0.20; even u lifted 2.91→2.49, bias −1.26→−0.94). @100→@1000 went the *right* way (1.5240→1.4738) — atypical, but the win holds massively either way. ⚠️ Still 2→7; NOT comparable to the original 2→10 champion (1.86) — 101 tests that. |
| 101 | _TBD_ | **1.4640 (1000, 17589 pts, 2→10)** / 1.6222 (100) | **★ UNCONDITIONAL CHAMPION (superseded by 104).** HORIZON 8 (2→10) carrying the 100 winners — TARGET_HORIZON_DAYS 5→8, **rollout off, delta off, anchor off, quantile head**, sparse, waist 32, batch 5 | **Beats the original 080 champion (1.8582 @1000, same 2→10 region) by −0.394 mag.** The delta-off + rollout-off wins are NOT artifacts of the easier 2→7 region — they carry to the full horizon. Like 100, @100→@1000 went the *right* way (1.6222→1.4640): the model generalizes to the full set better than the subset. Biases confirm the mechanism at the hard horizon: **u collapsed −1.0/−1.3 → −0.67**, i −0.07, z −0.07, r −0.16. Residual offenders ztfg −0.75 / g −0.64 = the genuine blue-band info limit in the 7→10 d tail. Eval used `--late_time_max_days 10.0` (default restored); plot default `--fixed_forecast_max_days 8`. |
| 102 | _TBD_ | **1.4562 (1000, 17589 pts, 2→10)** | **DENSE-CONTEXT twin of 101** (PROBE_DENSE_CONTEXT=True), else identical to 101 (waist 32, delta/anchor/rollout off, quantile, batch 5) | **Distillation re-test against the NEW floor.** After the floor moved ~0.4 mag (delta/rollout fixes), re-ran the go/no-go: does privileged dense in-window context help now? **Δ = −0.008 @1000 vs 101** (1.4562 vs 1.4640) — again statistically indistinguishable, deep inside the ±0.068 noise. **DISTILLATION STILL NO-GO**, now confirmed at the post-delta floor (was 094: Δ −0.006 at the old floor). The late-time signal is genuinely absent from the 2 d window regardless of sampling density — an information limit that survived the ~0.4 mag improvement. |
| 103 | _TBD_ | **1.4978 (1000, 17589 pts, 2→10)** | **WAIST-8 BYPASS CONTROL.** 101 with BYPASS_CHANNELS 32→**None** (waist 32→8), backbone still OFF, sparse, quantile, delta/anchor/rollout off, batch 5 | **Isolates the pure waist-narrowing cost** so the backbone-on run (104, forced to waist 8) has a fair matched-waist partner. **Δ = +0.034 @1000 vs 101** (1.4978 vs 1.4640) — narrowing the waist 32→8 costs a small but real 0.034 mag (just outside noise). This is the capacity penalty any backbone-on run must first recover before it can claim a net win. Biases mostly negative (u −0.56, g −0.44, r −0.35, z −0.21) — mild under-fade from the tighter waist. **103, not 101, is the correct comparison for 104.** |
| 121 | _TBD_ (ran as run-dir study_121) | **1.6617 (1000, 17589 pts, 2→10) — REGRESSED +0.28 vs 111. Anchor dead, but ztfg-specific finding is real.** | **FADE-ONLY TREND ANCHOR.** Champion 111 config EXACTLY (decoder scope, waist 8, N_QUANTILES=3 weights (1,3,1), color head OFF, rollout off, batch 5, CONTEXT_WINDOW_DAYS=2, DROP_UPPER_LIMITS=True, DT_WEIGHT_GAMMA=None) + THREE linked changes that together form ONE lever: `PREDICT_DELTA=True`, `TREND_DECAY_ANCHOR=True`, and the NEW `TREND_FADE_ONLY=True` (TREND_SLOPE_K=3, TREND_MAX_OFFSET=1.0). The head predicts a residual on a per-band anchor = last-obs + slope·Dt, but the slope is clamped NON-NEGATIVE (`slope.clamp_min(0)` in `_band_anchor`, bomberman.py) so only FADING bands lean into their trend; RISING (pre-peak, neg-slope) bands hold FLAT at v_last. New flag threaded through checkpoint meta + all 3 load sites (default False = symmetric/legacy, strict-loads). Fresh train. | **Physical hypothesis:** the 2→10 d forecast region is post-peak decline, so an in-window RISING band is pre-peak; its rise turns over at peak and extrapolating it forward overshoots (too bright) — the exact under-fade the symmetric anchor caused. Fade-only keeps the useful fade lean-in while removing the doomed-rise overshoot. **HONEST PRIOR (do not over-read):** study 100 showed removing the delta anchor entirely was the SINGLE BIGGEST WIN of the campaign (−0.316 @1000) because even flat-hold pinning to the bright near-peak last obs caused chronic under-fade; 097 showed the SYMMETRIC trend anchor was a constraint (−0.099 vs anchor-off). So re-enabling delta+anchor is reintroducing the largest known regression source — BUT the fade-only clamp is a genuinely NEW variant never tested (096/097 used symmetric slope), and it directly attacks WHY the anchor hurt. Realistic target = the FADING blue bands (u −0.31, g under-fade) that DO decline; it will NOT fix the ztfg plateau (flat in-window slope, nothing to lean on). **Read @1000 (seed ±0.02) vs 111 (1.3800): (1) overall < 1.3800? (2) do u/g/fading-band biases move toward 0? (3) does pinning rising bands flat regress anything?** If it regresses like 097/100 → the delta-pin damage dominates the fade-only benefit → anchor stays dead, accept it. **RESULT: REGRESSED +0.28 @1000 (1.6617). Anchor stays dead — reverted to champion (PREDICT_DELTA/TREND_DECAY_ANCHOR/TREND_FADE_ONLY all False). BUT the per-band split is the most informative anchor result yet and confirms BOTH hypotheses: (1) THE PHYSICS HELD FOR ztfg — RMSE 2.01→1.77 (BEST ztfg EVER) and bias −0.84→−0.20, the plateau eased because the fade-only anchor gave ztfg a fading reference instead of its bright plateau. So ztfg's plateau CAN be moved by anchoring (color could not, 117/118). (2) THE 100/097 DELTA-PIN DAMAGE ALSO HELD — every OTHER band regressed with incoherent bias-sign flips (g +0.09→−0.64, i −0.01→−0.39, r/ztfi/z/y all swung, u RMSE 2.16→2.80). Re-enabling the delta parameterization pins the well-behaved bands to their near-peak last obs and drags them off the clean absolute-head fade they had at 111; the fade-only clamp fixed rising-band overshoot but not the core delta-pin problem study 100 identified. NET: real ztfg win (~0.24) swamped by 6-band regression → +0.28. CONCLUSION: the anchor is a WRONG-GRANULARITY tool — it helps the ONE band whose last obs is a bad reference (ztfg plateau) and hurts the many whose absolute head was already good. The useful nugget: whatever fixes ztfg must apply ONLY to ztfg (a per-band mechanism), not a global head/anchor change that touches all 9. Points at a ztfg-targeted bias/level correction, NOT another global architecture lever.** |
| 119b | _TBD_ (ran as run-dir study_119) | **1.4177 (1000, 17589 pts, 2→10) — NO-OP. Lands on 106 (1.4178) / the ~1.40–1.42 champion-family seed center.** | **LEAD-TIME LOSS WEIGHTING, DOWN-WEIGHT LATE (gamma=−1).** Champion 111 config EXACTLY (single-step n_rollout_steps=1, decoder scope, waist 8, N_QUANTILES=3 weights (1,3,1), delta/anchor/rollout off, batch 5, BAND_WEIGHTS unchanged, DROP_UPPER_LIMITS=True) + SOLE change `DT_WEIGHT_GAMMA=-1.0` (DT_WEIGHT_TAU=3.0). Applies per-sample backward weight `(1+Dt/3)^-1` = the rollout path's `1/(1+Dt/tau)` shape, giving the short-lead rise relatively more gradient. `dt_weight_gamma` threaded into the single-step 9band epoch (loderunner.py) + checkpoint meta; None/0.0 = byte-identical champion (no loader change, train-only knob). Fresh train. | **The B-arm of the sign A/B.** The rollout studies (783–791 rationale) found equal-per-step weighting lets the over-represented flat late tail dominate → forecast collapses to a plateau, and `1/(1+Dt/tau)` fixed it there. This tests whether that mechanism transfers to the single-step champion path. **Read @1000 (seed ±0.02) vs 111 (1.3800).** See memory `kn-model-priors-poor-track-record`. **RESULT: NO-OP. Overall 1.4177 @1000 — indistinguishable from 106 (1.4178) and inside the ~1.40–1.42 seed center (110 finding); the −0.038 gap to 111's 1.3800 lucky draw is SEED, not this lever. Biases still champion-family under-fade (ztfg −0.84, u −0.31, deep bands near-centered). CONCLUSION: the single-step champion path does NOT suffer the rollout's over-represented-late-tail problem (target Dt is already ~uniform via horizon-covering sampling), so removing late weight changes nothing. The rollout's `1/(1+Dt/tau)` win did NOT transfer.** |
| 119a | _TBD_ (ran as run-dir study_120) | **1.4457 (1000, 17589 pts, 2→10) — REGRESSED +0.066 vs 111, +0.028 vs 119b (real, beyond ±0.02).** | **LEAD-TIME LOSS WEIGHTING, UP-WEIGHT LATE (gamma=+1).** Champion 111 config EXACTLY (as 119b) + SOLE change `DT_WEIGHT_GAMMA=+1.0` (DT_WEIGHT_TAU=3.0). Per-sample backward weight `(1+Dt/3)^+1` → late (long-lead) samples get up to ~3.7× the early weight. Fresh train. | **The A-arm; the naive "error grows with lead" hypothesis.** The dense `latetime_rmse_vs_lead.png` shows RMSE rising 1.2→1.9 with lead, so up-weighting late leads *seems* right — BUT this CONTRADICTS the rollout finding that late-weighting worsens the plateau. Higher regression risk; included so the numbers, not my prior, pick the sign. **RESULT: REGRESSED and made the target WORSE. Overall 1.4457 @1000 (+0.066 vs 111, +0.028 vs 119b). ztfg bias −0.44(111)→−0.84(γ−1)→−1.15(γ+1) MORE bright-biased; u −0.59, ztfr/ztfi both worse. MECHANISM: ztfg's LATE training samples are the bright ~21 plateau — faint truth is past the ZTF floor and DROPPED (DROP_UPPER_LIMITS=True). Up-weighting late leads just pours more gradient onto ztfg's WRONG (plateaued) targets → reinforces the plateau. Gradient reallocation across leads CANNOT fix an information-limited tail (same lesson as `aux_loss_precheck.py`). The 'error grows with lead' curve was confounded: late error is high because late is info-limited, not under-weighted.** |
| — | — | — | **THREAD CLOSED: lead-time loss weighting (119a/119b) is DEAD both signs. γ=−1 no-op (1.4177), γ=+1 regressed (1.4457). Champion stays 111 (1.3800). The `DT_WEIGHT_GAMMA` knob is retained (default None = champion) but not a lever. Root reason both fail: reweighting WHICH leads get gradient doesn't add information to the faint tail, and up-weighting late actively reinforces the dropped-UL plateau. Reverted config to DT_WEIGHT_GAMMA=None.** |
| 106 | _TBD_ | **1.4178 (1000, 17589 pts, 2→10)** / 1.5186 (100) | **★ NEW UNCONDITIONAL CHAMPION. FINE-TUNE SCOPE = "decoder"** — 104 champion with the SOLE change BACKBONE_FINETUNE_SCOPE tail→decoder (unfreezes bottleneck_stage4 + up_stage1's 8 SwinEncoder2 attention blocks + all up_connect + all PatchExpand + linear4unpatch @ 0.1× head LR; encoder still frozen). Backbone ON, waist 8, quantile, delta/anchor/rollout off, batch 5 | **Δ = −0.011 @1000 vs 104** (1.4178 vs 1.4287) — unfreezing the whole decoder beats the thin tail once the confounds are removed. **OVERTURNS the 085>084 prior** (which said tail>decoder): that comparison was batch-2 + delta-on + old-floor; at batch 5, delta-off, new floor the verdict reverses. **@100 MISLED, @1000 was the arbiter:** 106 @100 (1.5186) was WORSE than 104 @100 (1.4876), but 106's @100→@1000 tightening (−0.101) nearly doubled 104's (−0.059) → decoder scope generalizes to the full set better than tail. The feared overfitting (more params vs an information-limited signal) did NOT materialize at full eval. Biases improved: u lifted (RMSE 2.37→2.21, bias −0.30→−0.14), ztfr/ztfi/y near-centered; residual under-fade now ztfg −0.44, i −0.44. **Lesson reinforced: never call a scope/config verdict on @100 alone.** |
| 118 | _TBD_ | **1.4610 (1000, 17589 pts, 2→10) — REGRESSED +0.081, WORSE than 117. Mechanism did not fire; revealed the real defect.** | **Dt-INDEPENDENT COLOR CODE (fix study 117's time-escape hatch).** Study 117 config EXACTLY (COLOR_ANCHORED_HEAD, COLOR_SED_RANK=2, champion-111 base: decoder scope, waist 8, N_QUANTILES=3, delta/anchor/rollout off, batch 5, DROP_UPPER_LIMITS=True) + the SOLE change: new flag `COLOR_SED_DT_INDEPENDENT=True` makes the SED-code branch (`sed_head`) read ONLY the pooled backbone summary (`pool_channels`), NOT the Fourier lead-time encoding (`dt_extra`). The pivot (`ref_head`) still sees full `head_in` (carries all lead-time fade). So the per-object color is FIXED across Dt; the fade must flow entirely through the shared pivot. New flag threaded through checkpoint meta + all 3 load sites (default False → 117 behavior, strict-loads). Fresh train (sed_head first-layer width changes). | **Targets 117's PROVEN failure mode #1.** 117 regressed +0.054 with a diagnostic bias signature: 8 bands flipped to OVER-fade (+0.2–0.4), ztfg alone stayed UNDER (−0.57), ztfg RMSE unmoved (~1.9). Root cause: the rank-2 code consumed `dt_feat`, so it was constrained across BANDS but FREE across lead time — 2 dims easily encode "ztfg's color grows with Dt while others' don't," and single-band supervision on ztfg's plateaued targets (ZTF ~21 floor) taught exactly that → pivot fades, ztfg color un-fades to cancel → plateau reproduced. Removing Dt from the code forces ONE per-object color: ztfg can no longer time-escape; if the pivot fades (pinned by deep-band targets that DO fade) ztfg must fade with it. **Read @1000 (seed ±0.02), SAME 3 criteria: (1) overall < 111's 1.3800; (2) ztfg RMSE ~1.9 → toward ~1.0; (3) good bands not regressed.** Watch the bias signs specifically — if ztfg moves toward 0 while the 8 bands' over-fade shrinks, the mechanism fired. If ztfg still stuck, failure mode #2 (symmetric-pivot tug-of-war) dominates → next try = candidate (ii), anchor pivot on a deep band. See memory `kn-color-coupling-greenlit`. **RESULT: REGRESSED +0.081 @1000 (1.4610), WORSE than 117 (1.4340) AND 111 (1.3800). Mechanism did NOT fire — ztfg RMSE unmoved (1.8908→1.8772, ~0), bias −0.57→−0.43 (marginal). SAME signature as 117 but more extreme: ztfg alone stays UNDER-fade (−0.43), the BULK over-fades (g +0.29→+0.73, u −0.37→+0.47 flipped hard, ztfr +0.25). This ISOLATES failure mode #2 as the true blocker: removing the Dt escape hatch did nothing for ztfg and made the tug-of-war WORSE, because now the ONE per-object color must also absorb what the Dt-freedom used to → the symmetric pivot is dragged even harder by the majority. DEEPER CONCLUSION (the studies 117+118 pair proves it): under SINGLE-BAND supervision the shared pivot has NO gradient path that coobserves ztfg-vs-deep-band at the same (obj,phase) — each sample supervises one band in isolation, so 'ztfg should track z' is never in any loss term. The architecture CAN represent the coupling but training never asks for it; SGD just balances 9 independent marginals through a shared trunk → the majority wins and the outlier is abandoned. Both #1 (117) and the pivot symmetry (118) are downstream of this. Candidate (ii) (anchor pivot on a deep band) may still help by biasing the outlier toward a good band, but the ROOT fix is candidate (iii): an explicit cross-band color-consistency AUX LOSS at matched (obj,phase) — the exact quantity the ceiling test measured and the only thing that puts 'ztfg vs its neighbors' into a gradient. Champion stays 111 (1.3800). Recommend (iii) next, optionally stacked with (ii).** **[UPDATE — candidate (iii) KILLED before build by a read-only pre-check, `aux_loss_precheck.py`.]** |
| (pre-check) | — | **Candidate (iii) aux-loss pre-check: DO NOT BUILD. Color thread CLOSED.** | **`aux_loss_precheck.py` — read-only, dense pool, no training.** Asks the one question the original ceiling test skipped: can a color→ztfg target learned where ztfg is DETECTABLE (bright, ≤20.8 floor) transfer to the FAINT regime training never observes? (A color-consistency aux loss must build its target from bright ztfg; ZTF's ~21 floor means faint ztfg truth is never in training.) | **RESULT: color info HURTS the faint tail.** Q1 coverage 100% (anchor always present — not the blocker). Q2 ladder (faint-ztfg RMSE): predict-the-mean **0.556** < optimistic all-rows ceiling **1.056** < HONEST bright-fit→faint-test **1.332** < model **1.88**. The ordering is decisive: mapping deep-band mags through even the best color relation is WORSE than predicting a constant, because faint ztfg−neighbor COLOR scatters more than faint ztfg itself (the ~0.76 per-object color floor, now confirmed worse under an honest split). Pre-registered rule required honest ≪ marg std; got 1.332 vs 0.556 (2.4× WORSE) → **FAIL → do not build.** **Reframe (the useful part):** faint ztfg marginal std is only 0.556 → the failing points are TIGHTLY CLUSTERED, so the model's 1.88 is dominated by LEVEL/BIAS error (plateau too bright), NOT irreducible variance. The lever is whatever sets the absolute LEVEL, not cross-band color. **→ Re-elevates the redshift/distance scalar** (luminosity distance → apparent-mag offset directly pins the level); memory `kn-redshift-feature-idea`, ceiling-probe baseline study 104 (1.4287). Recommend an analogous redshift ceiling-probe BEFORE building. **Color-coupling thread (117 head, 118 Dt-indep, this ceiling re-read) DONE. Champion stays 111 (1.3800).** |
| 116/117 | _TBD_ | **1.4340 (1000, 17589 pts, 2→10) — REGRESSED +0.054, mechanism did NOT fire.** (ran as run-dir study_117) | **COLOR-ANCHORED SED-BOTTLENECK OUTPUT HEAD (new arch lever; UL thread abandoned).** Champion 111 config EXACTLY (decoder scope, waist 8, N_QUANTILES=3 levels (0.1,0.5,0.9) weights (1,3,1), POOL_MODE meanstdmax, DT_FOURIER_BANDS=8, PHASE_FOURIER_BANDS=10, delta/anchor/rollout off, CONTEXT_WINDOW_DAYS=2, MAX_CONTEXT_LEN=12, TARGET_HORIZON_DAYS=8, HIDDEN=128, batch 5, DROP_UPPER_LIMITS=True) + the SOLE change: replace the flat per-band output head `Linear(hidden → n_bands·n_quantiles)` (9 INDEPENDENT band reads) with a **shared abstract pivot + low-rank color code**. New model flags `COLOR_ANCHORED_HEAD=True`, `COLOR_SED_RANK=2`. Head = (1) `ref_head`: pivot m_ref [B,n_q], monotone via base+cumsum(softplus(gaps)); (2) `sed_head`: SED code z_sed [B,k], k=2, Dt/phase-dependent; (3) `W_band [9,k]` + `b_band [9]` Parameters → colors = mean-centered (b_band + z_sed·W_bandᵀ). Combine `pred = m_ref[:,:,None] + colors[:,None,:]` → [B,n_q,9]. Per-object band predictions live in a **(k+1)=3-dim SED subspace** vs full rank 9. Loss/eval/infer UNCHANGED (head still returns [B,n_q,9]; single-band one-hot masking, PinballLoss intact). FRESH train (new head params; backbone still loads pretrained). Checkpoint meta records `color_anchored_head`/`color_sed_rank`; all 3 load sites default False → legacy head, so 111 still strict-loads. | **Attacks the DOMINANT remaining error = band-STRUCTURAL, not capacity.** Dense late-time plots: model forecasts i/z/y correctly (they track the fade) but ztfg/ztfr/ztfi PLATEAU ~20-21 and flatline while truth fades to 26+ — ZTF's ~21 floor means the model NEVER sees faint ztfg detections, and the flat head lets ztfg's read decouple and plateau. **Color-correlation ceiling test (`color_correlation_test.py`, dense pool, object-grouped 5-fold ridge CV) GREENLIT this:** predicting ztfg from other bands at matched phase cuts late RMSE ~1.9 → **0.803 ceiling** (deploy variant, predictors masked below survey floor, **0.882** → recoverable from bands measurable late). A KN is ONE cooling SED through 9 filters, so faint ztfg is pinned by its measured neighbors via the (phase-evolving) color relation. **CRUCIAL no-op trap avoided:** training is single-band per sample, so a `m_ref + FREE color` head is algebraically identical to the flat head (no-op). Coupling only bites with a STRUCTURAL constraint → the rank-k SED bottleneck; the rank is architectural, un-tunable (unlike the reverted UL hinge which could be & was tuned away). When ztfg has no data its value is SET by the code inferred from the bands that do → the plateau can't form. **Read @1000 (seed noise ±0.02), pre-registered criteria: (1) overall late RMSE < 111's 1.3800; (2) ztfg RMSE ~1.9 → toward ~1.0 (mechanism target); (3) good bands u/g/r/i/z/y NOT regressed (the coupling must not drag calibrated bands — the failure mode the UL hinge showed).** Honest caveats: faint-regime ceiling 0.968 vs faint-subset marginal 0.756 → ~0.76 mag per-object color-scatter FLOOR; ceiling test used CONTEMPORANEOUS predictors (a live forward-forecast is harder), so expect gains SHORT of 0.80 — ztfg toward ~1.0-1.2 already a large win. If it clears → sweep COLOR_SED_RANK (1/3) as next single-variable A/B. See memory `kn-color-coupling-greenlit`. **RESULT: REGRESSED — all 3 criteria FAILED. Overall 1.4340 @1000 = +0.054 vs 111 (1.3800), ~2.7× the ±0.02 floor (real). (1) ztfg UNCHANGED/WORSE: RMSE 1.8908 (~1.9, no move), bias −0.26→−0.5726 (MORE bright-biased). (2) Good bands REGRESSED and their bias FLIPPED to over-fade: g +0.29, r +0.36, i +0.40, z +0.26, y +0.28, ztfr +0.18 (111 had these near-centered/under). u still −0.37. THE TELL: 8 bands went POSITIVE (over-fade), ztfg alone stayed NEGATIVE (under-fade). TWO linked failures. (a) TIME-ESCAPE HATCH: `sed_head` consumes `dt_feat`, so the rank-2 code — constrained across BANDS but NOT across Dt — is free to make ztfg's color GROW with lead time while others' don't. 2 dims is ample to encode 'ztfg un-fades as pivot fades'; single-band supervision on ztfg's plateaued targets (ZTF ~21 floor, DROP_UPPER_LIMITS=True) taught exactly that → plateau reproduced. (b) SYMMETRIC-PIVOT TUG-OF-WAR: a mean-centered shared pivot is dominated by the 8 well-behaved bands; to drag the ONE outlier (ztfg) fainter it must lower the pivot, which pushes the other 8 too faint. It CANNOT fix the outlier without hurting the majority, so SGD split the difference — everyone slightly worse, ztfg still broken. Root cause the ceiling test missed: it used CONTEMPORANEOUS predictors (the correct deep-band value present at readout); a forward-forecast has NO training signal that actually PULLS ztfg down — ztfg's own bright plateaued samples + a Dt-flexible color win the tug-of-war over the deep bands' faint samples. Champion stays 111 (1.3800). NEXT (candidates, not yet chosen): (i) make the color code Dt-INDEPENDENT (feed sed_head only the pooled backbone summary, NOT dt_feat) so the SED shape can't time-escape — forces a single per-object color the deep bands' fade must carry; (ii) ANCHOR the pivot on a well-forecast deep band (e.g. z or i) instead of a symmetric abstract mean, so ztfg is a color OFFSET from a band that already fades correctly, breaking the tug-of-war; (iii) reconsider whether single-band supervision can EVER couple forward — may need an explicit color-consistency auxiliary loss across bands at matched (obj,phase), which the ceiling test actually measured.** |
| 115 | _TBD_ | **1.5234 (1000, 17589 pts, 2→10) — REGRESSED HARD, worse than BOTH 114 and 111. Thread declared done.** | **CAP UL SAMPLE FRACTION (fix the 114 mix regression).** Study 114 config exactly (censored UL targets, UL_WEIGHT=0.5, decoder scope, waist 8, N_QUANTILES=3, delta/anchor/rollout off, batch 5) + the SOLE change: new dataset knob `UL_MAX_FRAC=0.10` caps censored-UL samples to ≤10% of detection samples via a **deterministic even-spaced subsample** (no RNG → identical across DDP ranks; detections always kept in full). Recorded in checkpoint meta (`ul_max_frac`). Train-only (val stays detections-only). | **Isolates the hinge's benefit from the sample-mix cost.** 114 diagnosis: the dataloader draws a FIXED `train_batches`/epoch with `drop_last=True`, so every UL sample added to the pool DISPLACES a detection from the epoch; ULs cluster in ZTF/red bands → starved the blue-band (u/g) detections (u 2.15→2.23, g 1.69→1.77 — exactly the bands the audit said censoring CAN'T help, so the regression there is pure mix-shift, not the loss). Bounding the UL share to 10% preserves the detection mix while keeping the strong-signal ztfg/red ULs (44%/40%+ violation) in the pool. **Hypothesis: ztfg keeps its 114 gain (1.86→~1.72) WITHOUT the u/g regression → net win vs 111.** Read @1000; watch (a) does overall drop below 111's 1.3800, (b) does ztfg hold its improvement, (c) do u/g recover to ~111 levels. If u/g recover but ztfg's gain also evaporates, the ztfg win was itself mostly mix-driven (a caution). If it clears → sweep UL_MAX_FRAC (0.05/0.20) or escalate UL_WEIGHT. NOTE: band_weights still ×2 ztfg ULs in the backward — a second-order compounding not addressed here (kept single-variable). **RESULT: REGRESSED HARD — all three criteria FAILED. Overall 1.5234 @1000 = +0.144 vs 111 (1.3800) and +0.105 vs 114 (1.4187) — WORSE than both the champion AND the uncapped run it was meant to fix. ztfg (target band) went to its most bright-biased state yet: RMSE 1.86→1.9924, bias −0.26→−0.89 (worse than 114's −0.43, near 113's −1.28 catastrophe). u/g did NOT recover — they got WORSE (u 2.23→2.28, g 1.77→1.95). Cap code verified correct (samples & sample_is_ul subset with identical keep_pos, no desync; commit title "reweight bands" is a MISNOMER — diff confirms UL_MAX_FRAC is the sole functional change, band_weights untouched). PARADOX: reducing the UL lever 10× should interpolate TOWARD 111, not overshoot ~0.14 past it. This FALSIFIES the 114 "sample-mix cost" diagnosis: if u/g regression were ULs starving detections, cutting ULs 10× would recover u/g — it did the opposite. Leading (unconfirmed) read: the censored-UL hinge itself perturbs the whole solution; making ULs a sparse over-weighted minority (UL_WEIGHT=0.5 held over ~10× fewer samples) destabilized the median rather than gently correcting it. **UL-as-censored-target thread declared DONE (114 mild regress, 115 hard regress). Champion stays 111 (1.3800).** Next lever = redshift conditioning scalar (memory kn-redshift-feature-idea; ceiling-probe baseline study 104 = 1.4287).** |
| 114 | _TBD_ | **1.4187 (1000, 17589 pts, 2→10)** — ztfg IMPROVED, overall slightly REGRESSED (mix cost) | **UPPER LIMITS AS CENSORED TARGETS (Option 3 — the "do it right" follow-up).** 111 champion config (QUANTILE_WEIGHTS (1,3,1), decoder scope, waist 8, N_QUANTILES=3, delta/anchor/rollout off, batch 5) + the SOLE change: admit late-time non-detections (upper limits) as **extra supervised TARGETS carrying a one-sided (censored/hinge) loss**, while the CONTEXT stays **detections-only** (byte-identical to the 111 champion → architecture unchanged; here trained FRESH, but could warm-start). New dataset flag `ul_as_censored_target` (distinct from & mutually exclusive with the reverted 113 `ul_as_flagged_context`): two-stream build — detections in `events_per_file` (t0/normalization from detections only, matching champion), ULs in a parallel `uls_per_file` that NEVER enters the context window. Each late-time UL becomes its own sample (anchor = most recent prior detection) and `__getitem__` returns a uniform 5-tuple `(x, target, mask, Dt, is_ul)`, `is_ul=1` for UL samples. Loss branch (epoch fn `_apply_ul_hinge`): UL samples use `UL_WEIGHT * relu(limit_z − median_pred_z)` on the observed band (median quantile), detection samples keep the masked pinball; combined via `torch.where`. **UL_WEIGHT=0.5**, `UL_TARGET_HORIZON_DAYS=2.0` (only ULs with lead >2 d are targets; ULs past `TARGET_HORIZON_DAYS`=8 dropped, not capped). **Validation stays detections-only** (champion-parity yardstick; hinge is train-only). Checkpoint meta records `ul_as_censored_target`/`ul_weight`/`ul_target_horizon_days`. | **Fixes the 113 one-sided-bound trap at its root: a UL is a CONSTRAINT, not a feature — enforce it in the LOSS, never the input.** `relu(limit_z − median_pred_z)` = 0 when the forecast already plateaus fainter than the limit (no gradient), positive only on VIOLATIONS (forecast too bright). **Audit (audit_upperlimit_censoring.py) GO before committing:** the 111 champion violates 21.4% of late-time ULs (1770/8281) — **ztfg 43.9% (846 pts = 48% of all violations, median depth 0.44 mag)**, i 40.2%, z 51.2%, y 51.6% — so the hinge has real gradient exactly on ztfg's under-fade, in the right direction (push fainter). u/g are ~4-6% (already respected → censoring can't help them; their RMSE is scatter, not bright-bias). **Read @1000, watch ztfg RMSE/bias specifically.** Δ ≲ 0.03 vs 111 (1.3800) ⇒ inside seed floor → confirm with a re-run. Failure mode to watch: 0.5 weight over-fading detections near the limit (blue-band bias creep) or destabilizing the median. If it clears noise AND helps ztfg → escalate UL_WEIGHT (1.0) as the next single-variable A/B. Rollout UL support intentionally not wired (n_rollout_steps==1 only). **RESULT: MIXED — mechanism validated, not a net win. Overall 1.4187 @1000 = +0.039 vs 111 (1.3800), ~2× the ±0.02 floor (real, mild). BUT the target band moved the RIGHT way: ztfg RMSE 1.86→1.7176 (−0.14). The regression is elsewhere and diagnosable: u (2.15→2.23), g (1.69→1.77), r (→1.51) all worse — precisely the bands the audit said censoring CAN'T help (4-6% violation), so this is NOT the loss misfiring but a SAMPLE-MIX shift: under fixed-batch training (fixed `train_batches`/epoch, `drop_last`) every UL sample displaces a detection, and ULs cluster in ZTF/red → blue-band detections starved. Note ztfg bias went MORE negative (−0.26→−0.43) while RMSE fell → the win is scatter reduction, not bias correction (don't over-read). Unlike 113 this is a real advance (1.4187 vs 1.5199, and it HELPED the target instead of destroying it). Pre-registered gate was "clears noise AND helps ztfg"; ztfg helped, overall didn't clear → do NOT auto-escalate weight. Next: cap the UL fraction (study 115) to remove the mix cost while keeping the hinge.** |
| 113 | _TBD_ | **1.5199 (1000, 17743 pts, 2→10) — REGRESSED, REVERTED** | **UPPER LIMITS AS FLAGGED CONTEXT (Option 1).** 111 champion config (QUANTILE_WEIGHTS reverted to (1,3,1), decoder scope, waist 8, N_QUANTILES=3, delta/anchor/rollout off, batch 5) with the SOLE change: admit non-detections (upper limits) into the event stream as **context only, never targets**, plus a new per-event `is_upper_limit` channel. Per-event layout grows `[value, rel_t, valid, one_hot_band]` (3+n_bands) → `[value, rel_t, valid, is_upper_limit, one_hot_band]` (4+n_bands). New dataset flag `ul_as_flagged_context`; model flag `upper_limit_channel` (recorded in checkpoint meta, auto-detected by eval/infer). **FRESH RETRAIN** (reshaped first Linear, no warm start). Normalization stays **detections-only** (a limiting mag is a one-sided bound, not a measurement). | **Mechanism target: ztfg** (worst band, RMSE ~1.86, plateaus ~4 mag bright at late phase). ZTF bands hit their detection floor earliest → carry the most upper limits, which the pipeline had been *discarding* (`DROP_UPPER_LIMITS`) — throwing away real "fainter than X by time T" late-time constraints, exactly the signal ztfg lacks. Flipping the flag naively is HARMFUL (ULs become supervised targets = train to predict the limiting mag as truth, and get read as measurements in context = bias brighter); hence context-only + flag + target-exclusion. **Read @1000, watch ztfg RMSE/bias specifically.** Δ ≲ 0.03 vs 111 (1.3800) ⇒ inside seed floor → needs a confirming re-run; watch other bands for regression from the shifted event mix. If it clears noise AND helps ztfg, the "do it fully right" follow-up is Option 3 (censored one-sided pinball loss on the ULs). Rollout mode UL support intentionally guarded off (n_rollout_steps==1 only). **RESULT: REGRESSED, mechanism backfired — REVERTED.** Overall 1.5199 @1000 = **+0.14 vs 111 (1.3800), ~7× the ±0.02 seed floor** → real, not seed. **ztfg (the target band) got WORSE: RMSE 1.86→2.12, bias −0.26→−1.28** — a much harder UNDER-fade (predictions far too bright), the OPPOSITE of the intended effect. Collateral: r/z biases swung strongly positive (+0.43/+0.33), whole event mix perturbed net-worse. **Diagnosis = the one-sided-bound trap** flagged in the plan: a ztfg UL ("fainter than ~mag 20.5") carries a limiting-mag VALUE far brighter than the true late-time source (~mag 26), fed into context z-scored with detection stats. A bare `is_upper_limit` flag was NOT enough for the model to treat it as a lower bound on faintness only — it partly read the value as a soft measurement and pulled forecasts bright. ZTF has the most ULs + shallowest floor → ztfg hit hardest. **Option 3 (censored one-sided pinball loss) was gated on Option 1 clearing noise AND helping ztfg; it did NEITHER, so we do NOT escalate.** Reverted `UL_AS_FLAGGED_CONTEXT=False` (all plumbing dormant, back to 111 detections-only champion). Also fixed a latent bug found via this run: the continuation-restart loader (`load_direct_loderunner_checkpoint_9band`) didn't read `upper_limit_channel` from checkpoint meta → strict-load shape mismatch (194 vs 182) on cycle restart; now threaded like every other arch flag. **Lesson: a scalar flag can't teach the model the semantics of a one-sided bound whose value lies on the wrong side of the truth — the bound must be enforced in the LOSS (censored), not hinted in the input.** |
| 112 | _RUNNING_ | _TBD_ | **MEDIAN-WEIGHTED PINBALL, PUSHED** — 111 with QUANTILE_WEIGHTS (1,3,1)→(1,8,1), normalized (0.2,0.6,0.2)→(0.1,0.8,0.1). Median now 0.8 of the combine vs 0.1 each outer. Sole change vs 111; else 106 champion (decoder scope, waist 8, N_QUANTILES=3, delta/anchor/rollout off, batch 5) | **Tests whether the median-weight mechanism keeps paying.** 111 (1,3,1)=1.3800 with best-ever u (2.15) and a systematic under-fade shift → real signal, but ~0.02 vs the honest ~1.40 center sat near the seed floor. Pushing to (1,8,1) does double duty: (a) if u/g improve further, the effect is real AND we gain more; (b) a monotonic 111→112 drop is strong evidence the gain is NOT seed (two independent runs trending the same way is hard to explain by RNG). **Watch the failure mode: 0.8 median weight leaves only 0.1 on each outer quantile — if they decalibrate, the point forecast loses their regularization and the blue bias can creep back (098: bare point head reintroduced blue bias). If 112 REGRESSES vs 111, we've bracketed the optimum → (1,3,1) stands and a confirming re-run of 111 is the move.** @1000 arbiter. |
| 111 | _TBD_ | **1.3800 (1000, 17589 pts, 2→10)** — LOWEST YET, but partly seed (needs re-run) | **MEDIAN-WEIGHTED PINBALL — PROMISING, NOT CONFIRMED.** 106 champion with the SOLE change QUANTILE_WEIGHTS=(1,3,1)→normalized (0.2,0.6,0.2) in the pinball combine (was equal .mean() = (⅓,⅓,⅓)). Decoder scope, waist 8, N_QUANTILES=3, delta/anchor/rollout off, batch 5, EMA off. **Two comparisons diverge: −0.0378 vs 106 (1.4178, clears ±0.02 floor) but only −0.0193 vs 110 (1.3993, same config as 106 at a diff seed = the honest ~1.40 center → INSIDE the floor).** 106 was a mildly unlucky draw, so the true gain is ~0.02, right at the noise edge — needs a confirming re-run. **BUT the biases show a real mechanism, not just seed:** u (dominant offender) RMSE **2.15 = best ever** (106 2.21, 110 2.41), MAE 1.52 (110 1.80); g 1.69 (106 1.75). The whole bias vector went uniformly gentle-negative (−0.14 to −0.53) — a SYSTEMATIC under-fade shift (fingerprint of the median weighting working), unlike 110's seed-scatter (u −0.99 but g/r/z/y ~0). So: median upweight is doing real work on the blue-band tail; top-line ~0.02–0.04 is part real, part favorable seed. Reallocation mechanism: pinball trains 3 quantiles independently, equal combine spent ⅔ of gradient on the unscored q10/q90 band, only ⅓ on the median that drives RMSE. Weights normalized to sum 1 → LR unchanged (uniform reproduces old .mean() to allclose). **NEXT: confirm with a re-run, and/or push to (1,8,1) — but watch outer-quantile decalibration (098: bare point head reintroduced blue bias).** Impl: PinballLoss gained optional quantile_weights arg (loderunner.py), default None = byte-identical legacy. |
| 110 | _TBD_ | **1.3993 (1000, 17589 pts, 2→10)** — raw AND --use_ema IDENTICAL | **EMA decay 0.1→0.99 — NO-OP; also the campaign's lowest number, but the win is SEED, not EMA.** 106 champion config (decoder scope, mult 0.1, 40 ep) with ema_decay 0.1→0.99, sole change. **Paired same-checkpoint eval: raw 1.3993 / --use_ema 1.3993** (RMSE identical to 4 dp; MAE 1.0142 vs 1.0145; per-band diffs in 3rd–4th dp). **EMA does NOTHING here because the run PLATEAUED** (the flat loss curve): averaging a converged, near-stationary trajectory returns the endpoint. Polyak helps only when weights OSCILLATE around a minimum — not this trajectory. Raising decay→0.999 wouldn't help (nothing to average). **EMA dead as a lever for this model.** ⚠️ Since raw==ema and ema is the only change vs 106, study 110's raw weights ARE the 106 champion config at a different SEED → the −0.0185 vs 106 (1.4178) is **seed variance, well inside ±0.068 noise**, NOT a lever win. Do NOT crown a new champion off this. Real takeaway: **run-to-run seed noise on this config ≈ ±0.02; true champion-config perf ≈ 1.40–1.42; 106's 1.4178 was a mildly unlucky draw.** Biases: net under-fade (champion-family) — u worse (RMSE 2.41/bias −0.99 vs 106's 2.21/−0.14) but g/r/z/y centered (all |bias|<0.16); mixed pattern = seed, not signal. |
| 109 | _NOT RUN (source reverted)_ | — | **60 EPOCHS** (was planned on 108's 0.03 config; total_epochs 40→60 + terminal_steps ×1.5). Superseded before launch: 108's @1000 wasn't in, so we did not stack epochs on an unproven LR change. total_epochs reverted 60→40 for study 110. **Caveat preserved for any future 60-epoch run:** with num_cycles=0.5 the cosine RISES back toward anchor_lr past terminal_steps, so terminal_steps MUST scale ×1.5 or ep41–60 run at a climbing LR instead of annealed. |
| 108 | _RUNNING_ | _TBD_ | **BACKBONE LR MULT = 0.03** — 106 champion (decoder scope) with the SOLE change BACKBONE_TAIL_LR_MULT 0.1→0.03 (unfrozen decoder trains at 0.03× head LR instead of 0.1×; head = conditioner + output_head still at full anchor_lr). Backbone ON, waist 8, quantile, delta/anchor/rollout off, batch 5 | **The one LR knob with a mechanism.** 107 showed pretrained weights OVER-move at 0.1× (bias flipped under→over-fade once the encoder was unfrozen); this asks whether the DECODER itself also wants a slower rate. Also probes the loss-curve plateau (flat from ~ep15) — if the decoder was moving too fast, a slower rate may keep improving late. **Expected small (±0.01–0.02): the residual error is the info-limited blue-band tail, and LR polishes an optimization floor, it can't manufacture missing signal.** @1000 is the arbiter — do NOT call on @100 (106's @100 was misleading). |
| 107 | _TBD_ | **1.4279 (1000, 17589 pts, 2→10)** | **FINE-TUNE SCOPE = "full"** — 106 champion with the SOLE change BACKBONE_FINETUNE_SCOPE decoder→full (adds the last frozen block, the shared ENCODER: parallel_embed + var_embed_layer + agg_vars + pos_embed + temporal_encoding + dwn_stage1/2/3 + down_connect + PatchMerge, on top of the decoder set, so the ENTIRE pretrained backbone trains end-to-end @ 0.1× head LR). Backbone ON, waist 8, quantile, delta/anchor/rollout off, batch 5 | **NO-GO — ladder bottoms out at decoder. Δ = +0.010 @1000 vs 106** (1.4279 vs 1.4178) — a mild regression, inside the ±0.068 noise but on the wrong side. Unfreezing the shared encoder at the same 0.1× mult did NOT help; the decoder scope (106) remains champion. **Ladder is now non-monotonic with a minimum at decoder:** tail 1.4287 → **decoder 1.4178** → full 1.4279. **Failure mode = bias sign FLIP:** 106 was net under-fade (negative biases, ztfg −0.44); 107 went net OVER-fade — u +0.47, g +0.54, y +0.42, ztfi +0.26, z +0.13, only ztfg −0.11. The encoder drifted off its pretrained feature extraction and started over-correcting the faint tail — the "encoder overfit/drift" risk, not a domain-gap win. **The shared encoder is best left FROZEN as a feature extractor** (vindicates the decoder-scope design intent). A future run could retry with a LOWER encoder-only mult (0.03) via a 3rd param group, but at equal LR the encoder is a net drag. |
| 105 | _TBD_ | **3.0752 (100, 1759 pts)** | **SPATIAL RENDER + INTERPOLATED CONTEXT** (RENDER_INTERPOLATE=True) — draw each band's context as continuous piecewise-linear segments between detections instead of sparse tent dots; backbone ON + tail unfrozen, waist 8, quantile, delta/anchor/rollout off, batch 5 | **DECISIVE NO-GO — densifying context does NOT rescue render.** Worse than even plain-tent render (086a 2.94, 088 2.41) and +1.6 mag above the 104 champion (1.4287) @100; no @1000 needed. The interpolation worked as built (verified 13→224 nonzero rows for a 3-detection band), so the failure is not the encoding — it's the **structurally-empty forecast region the gather still reads from**, exactly the caveat this run deliberately did not address. **Bias signature flipped to strongly POSITIVE** (all bands +0.6 to +1.9; g +1.74, y +1.94) vs the champion's negative under-fade: the fingerprint of the readout collapsing toward the normalized mean (row=0 ⇒ mean magnitude ⇒ far too bright vs the faint faded tail). Richer context gave the encoder a curve to look at but nothing to READ where it matters. **Confirms (3rd time) the render readout gap is the real defect;** the only untried fix is forecast-region PREFILL (persistence/trend prior into the readout rows), the deferred step. Interpolate-only lever = dead. |
| 104 | _TBD_ | **1.4287 (1000, 17589 pts, 2→10)** / 1.4876 (100) | **★ NEW UNCONDITIONAL CHAMPION.** = 103 + **BACKBONE ON, tail unfrozen** (BYPASS_BACKBONE=False, BACKBONE_TAIL_LR_MULT=0.1, scope="tail"), waist 8 (forced), sparse, quantile, delta/anchor/rollout off, batch 5 | **First time in the whole campaign the backbone HELPED.** Two clean reads: (1) **vs the matched control 103** (waist 8, backbone off): Δ = **−0.069 @1000** (1.4287 vs 1.4978) — at identical waist the unfrozen backbone tail adds real signal the bypass MLP cannot. (2) **vs old champion 101** (waist 32, backbone off): Δ = **−0.035** even while paying the +0.034 waist-8 penalty → the backbone contribution (~0.07) more than covers the capacity it gave up. **Overturns the 082/085/086 verdict** that backbone-on merely ties the MLP: that held under *frozen* constant-image mode; the *unfrozen tail* changes the story. Atypically, @1000 (1.4287) came in BELOW @100 (1.4876) — the @100 was pessimistic here. Biases: u −0.86 (still the dominant offender, RMSE 2.40), g −0.50, z −0.42; reds/y tight (r −0.15, i −0.18, y +0.20). Residual blue-band under-fade is where **redshift conditioning** should bite next. |

> ⚠️ **BATCH CONFOUND (studies 089–092).** All four ran at **BATCH_SIZE=2**
> (the reduced regime introduced for 084/085 to fit VRAM), while champion **080
> ran batch 5** (eff. 20). The batch cut alone costs ~0.045 mag (082→085), and the
> full batch-2 + shortened schedule costs ~0.30 here (092 vs 080). **Therefore any
> comparison of an 089–092 number to 080's 1.8318 is invalid** — the gap is mostly
> batch, not the variable under test. Within the batch-2 family the comparisons are
> clean: at matched regime **dense context ≈ or slightly beats sparse** (091 2.0555
> vs 092 2.1289), so the earlier "dense context hurts → distillation dead" call was
> an artifact and is **retracted**. **Resolved by 093+094:** the batch-5 sparse
> reproduction landed 1.8792 (Δ +0.021 vs 080) and its dense twin 094 landed 1.8733
> (Δ −0.006 vs 093) — batch was the whole confound, and at matched regime dense
> context ≈ sparse. Distillation is a clean **no-go** (see 094).

> 🔻 **HORIZON-REGION CHANGE (studies 095+).** Starting at 095 the scored region
> is **2 < phase ≤ 7 d** (was 2 < phase ≤ 10 d for ≤094). The shorter horizon
> scores an **easier, nearer** population, so its RMSE is **lower by construction**
> and is **NOT comparable** to the 080/093 champion (1.88 on 2→10) or any ≤094 row.
> Valid same-region comparisons among these runs are only **095 ↔ 096 ↔ 093′**
> (all 2→7). **Owed baseline — now RESOLVED (093′):** re-scoring the 093 checkpoint
> on 2→7 (no retrain) gave **1.8677**, only −0.012 vs its own 2→10 number → the
> 2→7 region is **not meaningfully easier**, so 096's 1.7896 is a **real** win
> (−0.078 vs the champion on the matched region), not a region artifact. Reference
> frames: eval
> `--late_time_max_days` is **phase from trigger**; train `TARGET_HORIZON_DAYS` is
> **lead from anchor**; phase = lead + CONTEXT_WINDOW_DAYS (2 d), so 7 d phase ↔
> lead 5.

**Eval-size note:** 100-object evals run ~0.03 mag optimistic vs 1000-object
(080: 1.8318 → 1.8582). Quote the **1000-object** number when comparing studies;
the 100-obj bootstrap CI half-width alone is ±0.068.

## Update (studies 095–096): scheduled sampling was hurting

**First lever to move the floor since 080.** Turning scheduled sampling OFF
(n_rollout_steps 12→1) improved late-time RMSE by **−0.078 mag** against the
champion on the matched 2→7 region (096 1.7896 vs 093′ 1.8677), confirmed two
independent ways: the direct 095↔096 twin (−0.127, rollout the only variable) and
096-vs-093′ (−0.078, same region). Root cause: 12-step rollout optimized for an
autoregressive inference path we never run — eval is **DIRECT single-pass**, so the
rollout training was a pure train/eval task mismatch, paying exposure-bias cost with
no matching benefit. The 2→7 region itself is not meaningfully easier (093′ vs 093:
−0.012), so the gain is real, not a region artifact.

**Caveat — specialist vs generalist.** 096 trains to horizon 5 (phase 7), so it is a
7-day *specialist* and cannot forecast 7→10 d; it wins **within the 7-day deployment
target**, not by dominating the horizon-8 generalist. **Next:** retrain rollout-off
at horizon 8 to claim the general champion (strong prior it lands below 093's 1.8792
on 2→10). Note TARGET_HORIZON_DAYS uniform-Δt multi-lead sampling is a SEPARATE
mechanism from rollout and is preserved at n_rollout_steps=1.

## Update (studies 097–100): the delta anchor was the floor — "aleatoric floor" LARGELY OVERTURNED

**The ~1.88 "aleatoric floor" was substantially self-inflicted.** Three stacked
levers, each an independent single-variable A/B, dropped late-time RMSE from the
096 champion (1.7896 @1000, 2→7) to **1.4738 @1000 (study 100)** — a **−0.316 mag**
improvement, the largest jump in the log:

1. **Rollout / scheduled sampling OFF** (095→096, −0.127): train/eval task match.
2. **Quantile head > point+Huber** (~−0.09, confirmed both delta on 096>098 and off
   099>100): the pinball 0.5 term controls blue-band bias; Huber δ=0.1 is an
   objective/metric mismatch (median-like objective, L2 metric).
3. **PREDICT_DELTA OFF** (096→100, −0.316 as the sole change; mirrored head-
   independently by 098→099 −0.089): **the biggest one.** The delta head pinned each
   forecast to the *bright near-peak last observation* in the 2 d window, structurally
   biasing against fading → the chronic under-fade behind every negative bias in
   081–094. The absolute head fades freely; study 100's @1000 biases are the most
   centered ever (ztfg −0.05, y −0.07, even u lifted −1.26→−0.94).

**What this means for the old conclusion below:** the "floor confirmed 5 ways"
narrative was measuring a floor created by the delta parameterization + rollout, not
a true aleatoric limit. Capacity/backbone/render/distillation are still closed (those
findings stand), but the model was **not** at the information limit — it was
mis-parameterized. **Distillation (094 no-go) should arguably be re-tested** now that
the sparse baseline moved ~0.3 mag.

**Flat-loss mystery resolved (098):** training was never dead — the ep3→ep40 eval
descended 2.34→1.70 mag. The flat *training-loss* curve was a plotting artifact:
normalized z-score units + log y-axis + Huber-capped tail. Loss plot default is now
**linear**.

**Caveat:** all of 095–100 are scored **2→7** and are NOT comparable to the original
2→10 champion (1.86). **Study 101** carries the 100 winners to horizon 8 (2→10) to
claim the unconditional general champion — pending.

## ★ CURRENT CHAMPION (study 106): 1.4178 @1000 on 2→10

**Config:** **backbone ON, DECODER unfrozen** (BYPASS_BACKBONE = False,
BACKBONE_TAIL_LR_MULT = 0.1, BACKBONE_FINETUNE_SCOPE = "decoder" — bottleneck +
whole decoder unfrozen @ 0.1× head LR, encoder still frozen), **waist 8** (forced by
backbone-on: BYPASS_CHANNELS = None), **quantile head (3, levels 0.1/0.5/0.9)**,
**PREDICT_DELTA = False** (absolute head), **TREND_DECAY_ANCHOR = False**,
**n_rollout_steps = 1** (scheduled sampling off), TARGET_HORIZON_DAYS = 8, sparse
context, BATCH_SIZE = 5. Eval on 2→10 with `--late_time_max_days 10.0`.

**Result: 1.4178 @1000, −0.011 vs the study-104 tail-scope champion** (1.4287).
Unfreezing the whole decoder (not just the thin output tail) is a small but real
win once the confounds are stripped. **Overturns the old 085>084 "tail beats
decoder" finding** — that was batch-2 + delta-on + old-floor; at batch 5, delta-off,
new floor the verdict reverses. **Methodological note: 106's @100 (1.5186) was WORSE
than 104's @100 (1.4876); only the @1000 revealed the win** (106 tightened −0.101
@100→@1000 vs 104's −0.059). The feared overfitting from more trainable params vs an
information-limited signal did not appear at full eval.

**Backbone-on lineage (all beat the bypass MLP):** 103 (waist-8 bypass control,
backbone OFF) 1.4978 → 104 (tail unfrozen) 1.4287 → 106 (decoder unfrozen) 1.4178.
This overturns the 082/085/086 verdict that the backbone merely ties the bypass MLP
(that held under *frozen constant-image* mode); unfreezing progressively more of the
decoder keeps helping.

**Predecessor context:** the ~1.88 "aleatoric floor" (studies 081–094) was NOT an
information limit — the delta persistence prior + scheduled-sampling mismatch
created it. Studies 096–101 removed both (rollout-off −0.127, quantile>Huber ~−0.09,
delta-off ~−0.32) to reach 1.4640; 104 then adds the backbone lever on top.

**Still open / next:**
- **Unfreeze scope ladder — SETTLED, minimum at decoder (106).** tail (104, 1.4287)
  → decoder (106, **1.4178, champion**) → full (107, 1.4279, regressed). Unfreezing
  the shared encoder at 0.1× hurt (bias flipped under→over-fade); the encoder is best
  left FROZEN. All three scopes are now wired in checkpointing.py. Only untried
  variant: a LOWER encoder-only mult (0.03) via a 3rd optimizer param group — low
  priority, at equal LR the encoder was a net drag. **The backbone-capacity lever is
  exhausted; the remaining error is information-limited (blue-band tail), not capacity.**
- **Blue-band tail** (ztfg −0.44, u RMSE 2.21 @2→10) is the dominant remaining
  error. The redshift conditioning-scalar idea targets it but is PARKED (may not be
  available for real events — deployability concern raised by user).
- **Distillation is settled NO-GO** at the new floor too (102: Δ −0.008 vs 101).
- **Spatial render is DEAD** (105: interpolate-only 3.08; the empty forecast readout
  region is the structural defect, only forecast-region prefill could fix it).

## Conclusion (as of study 094 — SUPERSEDED by studies 095–101; see above)

> ⚠️ The framing below is **retained for history but is WRONG.** The "aleatoric
> floor confirmed 5 ways" was measuring a floor created by the delta
> parameterization + rollout, not a true information limit. Study 101 (1.4640)
> broke it by −0.39 mag. The capacity/backbone/render/data findings still stand as
> individual results, but the *conclusion that ~1.88 was irreducible* does not.

Model is at the **aleatoric floor** (~1.88 late-time RMSE at 1000 objects),
confirmed **5 ways**: capacity (waist 32→64 flat, 083), backbone (082 tie), data
(no train/test gap), spatial render (086–088 all worse), and **context density**
(094: dense context = sparse to within −0.006, clean batch-5 test).

**Both lines closed:**
- **Spatial render (086–088): dead.** The gather reads a forecast region that is
  zero by construction (~0.44% of the field non-zero); a mostly-frozen encoder
  can't manufacture signal there. Best rescue (088, tail-unfreeze + trend anchor)
  still lost to plain bypass. Abandoned.
- **Distillation: NO-GO (clean call, study 094).** The 089–091 "no-go" was
  **retracted** as a batch artifact, then re-tested properly: 093 (sparse, batch 5)
  reproduced 080 at 1.8792, and its dense-context twin 094 landed 1.8733 — **Δ
  −0.006, indistinguishable.** Privileged dense context buys nothing, so a
  dense-context teacher is no stronger than the sparse student. The late-time
  signal is genuinely not in the 2 d window regardless of sampling density — an
  information limit. No pipeline to build.

**Champion: study 080** — bypass MLP, waist 32, quantile head, trend anchor
(1.8582 @1000), reproduced by **093** (1.8792 @1000) in the current code. Its
remaining error is likely (a) per-band systematic bias and (b) context CONTENT
(photometric errors, per-band recency). **Batch-5 is the reference regime** (084–092
were all batch-2 and are not directly comparable to it); 093 re-anchors it and is
the sparse baseline all future dense/distillation runs must be compared against.

\* Commit is the nearest committed state matching that study's config; the study
number itself is not in the commit (see note above). Where a study reused an
earlier commit's code with only a config toggle, the same hash is listed.

## Baseline map (quick reference)

- **075 champion (pre-quantile):** `e6883db` — bypass MLP, waist 32, point head.
- **080 champion (superseded):** `a1166b7` — 075 + quantile head; 1.8582 @1000.
- **101 champion (superseded):** bypass MLP, waist 32, quantile head, delta OFF,
  anchor OFF, rollout OFF; **1.4640 @1000 on 2→10.**
- **104 champion (superseded):** backbone ON + tail unfrozen (mult 0.1,
  scope="tail"), waist 8, quantile head, delta OFF, anchor OFF, rollout OFF;
  **1.4287 @1000 on 2→10.** First backbone-on win; beat control 103 (1.4978) by −0.069.
- **106 champion (current):** 104 with scope="decoder" (whole decoder + bottleneck
  unfrozen @ 0.1× LR, encoder frozen); **1.4178 @1000 on 2→10.** Beats 104 tail scope
  by −0.011. Won on @1000 despite a worse @100 — decoder scope generalizes better.
- **081/082 backbone experiments:** `9ef46e8` and later — backbone un-bypassed.
